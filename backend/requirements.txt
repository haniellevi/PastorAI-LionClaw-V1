# PastorAI backend — core dependencies (Backend Core sprint)
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.14.2,<3.0
SQLAlchemy>=2.0,<2.1
psycopg2-binary>=2.9,<3.0
httpx>=0.27,<1.0
PyJWT[crypto]>=2.8,<3.0
cryptography>=50.0,<51.0
redis>=5.0,<6.0
# IANA tz data p/ zoneinfo.ZoneInfo("America/Sao_Paulo") (Células PR2 / agenda).
# Necessário em hosts sem tz do SO (Windows, containers slim/Alpine).
tzdata>=2024.1

# Agente Orquestrador / LLM BYO (US-08/US-27 — delta-034)
langgraph>=1.0.10,<1.1
# Security floor for LangGraph's transitive telemetry client.
langsmith>=0.8.18,<0.9
openai>=1.30,<2.0

# Dev / test
pytest>=8.0
