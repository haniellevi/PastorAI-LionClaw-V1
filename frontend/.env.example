# Backend FastAPI (api-login, /auth/me) — SPEC 3.2 / 5.3
NEXT_PUBLIC_API_URL=http://localhost:8000

# Clerk (Auth — US-01 / RNF-01). Sem chave, o app roda em modo backend-only
# (login continua consumindo api-login, que valida via Clerk no servidor).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
