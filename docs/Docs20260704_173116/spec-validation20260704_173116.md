# Validation Report

## Status: PASS

## Completude (Discovery → SPEC)
Todos os itens do discovery estao cobertos na SPEC.

- US-01 a US-12 (Domínios A/B/C/D do PRD e stories) presentes na SPEC §1 e no Anexo de Rastreabilidade.
- 4 endpoints do RF-01 cobertos na SPEC §3.2: `GET /cells/{cellId}/reunioes`, `POST /cells/{cellId}/reunioes/next`, `POST /cell-reunioes/{reuniaoId}/presenca`, `POST /cell-reunioes/{reuniaoId}/expectativas-visitantes`.
- 3 tabelas novas (`celula_reuniao`, `celula_presenca`, `celula_expectativa_visitante`) especificadas na SPEC §2.1 com colunas, CHECKs, UNIQUEs e índices conforme RF-19..RF-22.
- RLS `tenant_isolation` nas 3 tabelas (RF-23/RNF-01) na SPEC §2.2; `set_tenant_context`/BYPASSRLS preservado (RNF-02).
- Serviço de cálculo da próxima reunião (parser PT-BR, regra de próxima data, helper de data injetável — RF-03/04/05/RNF-07) na SPEC §3.2.
- Stack (FastAPI + SQLAlchemy + PostgreSQL/Supabase + Clerk + RLS por tenant) idêntica entre PRD e SPEC.
- Postura backend-only / sem frontend (RNF-11, Escopo Negativo) registrada formalmente na SPEC §4 (FE-DEC-01) com mapa de consumo para PRs futuros.
- Cobertura de testes pytest (RF-28/29/30) refletida na estrutura de pastas da SPEC §3.1.
- RNF de segurança/multi-tenant/idempotência refletidos no checklist da SPEC §5.2.
- Decisões fechadas DEC-01/DB-DEC-01 (presença = `estado`+`origem`, não boolean) e DEC-02/DB-DEC-02 (expectativa nominal, não por contagem) respeitadas na SPEC §2.1 e §3.2.

## Consistencia Backend ↔ Frontend
Backend e frontend estao consistentes.

- O PR2 é backend-only; nenhuma tela/hook/rota é implementada. A SPEC §4.3 registra apenas o mapa de consumo para PRs futuros, usando os mesmos tipos do backend (`ReuniaoOut`, `PresencaOut`, `ExpectativaVisitanteOut`) em camelCase coerente com os schemas Pydantic.
- Tratamento de erros esperado (422 materialização, 403 membro comum, 404 fora do tenant, 200 idempotente na presença) descrito de forma coerente com o contrato do backend (SPEC §4.3/§4.6 vs §3.2/§5.1).

## Consistencia Database ↔ Backend
Database e backend estao consistentes.

- `ReuniaoOut` `{id, celulaId, data, hora, tema, status}` mapeia colunas de `celula_reuniao` (`celulaId` ← `celula_id`, camelCase).
- `PresencaOut` `{id, reuniaoId, pessoaId, estado, origem}` mapeia colunas de `celula_presenca`.
- `ExpectativaVisitanteOut` `{id, reuniaoId, pessoaId, nomeVisitante, observacaoOracao}` mapeia colunas de `celula_expectativa_visitante` (`nomeVisitante` ← `nome_visitante`, `observacaoOracao` ← `observacao_oracao`).
- Cada tabela tem endpoint que a consulta/grava: `celula_reuniao` (GET/POST reuniões), `celula_presenca` (POST presença), `celula_expectativa_visitante` (POST expectativas).
- UNIQUE de `celula_reuniao` na SPEC = `(igreja_id, celula_id, data, coalesce(hora,''))`, alinhado à decisão fechada DB-DEC-03 (que corrige o texto anterior `UNIQUE(celula_id, data)` dos RF-02/RF-19/RNF-05). A SPEC segue corretamente a decisão canônica — sem conflito.
- Idempotência da presença garantida por `UNIQUE(igreja_id, reuniao_id, pessoa_id)` com tratamento de `IntegrityError` → 200 (nunca 409), coerente entre SPEC §2.1/§3.2 e RF-11/RF-12/RNF-13.
- FKs ON DELETE CASCADE (DB-DEC-04) e validação explícita de tenant via `_assert_pessoa_tenant`/`_get_reuniao_or_404` (FK não RLS-scoped) coerentes com RF-07b/RF-14/RF-27.

## Resumo
- Total de issues: 0
