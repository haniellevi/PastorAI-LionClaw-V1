# PRD — PastorAI 1.0 (MVP)

> Documento de Requisitos de Produto consolidado a partir do discovery, das User Stories/Requisitos aprovados e do Design Lock travado (`design-contract.json`, `design-brief.md`, `design-lock-report.md`).
> O Design Lock (status **APROVADO**, travado em 2026-06-12) e a fonte oficial de telas, navegacao, componentes, estados de UI e dados exigidos pela interface.
> Idioma: portugues brasileiro.

---

## 1. Resumo Executivo

O **PastorAI** e um SaaS multi-tenant para igrejas no modelo de celulas G12 que transforma o WhatsApp oficial da igreja em um canal de atendimento, cadastro e acompanhamento conduzido por um agente de IA, somado a um painel web que organiza o trabalho pastoral como uma **fila de pendencias** ("o que exige acao hoje").

**Problema:** lideres e pastores perdem pessoas por falta de acompanhamento estruturado — visitantes sem celula, decisoes por Jesus sem consolidacao, relatorios de celula que nao chegam e atendimentos que ficam sem resposta. A rotina e operacional, dispersa em planilhas e conversas pessoais.

**Solucao (MVP):**
- **Atendimento por IA no WhatsApp oficial:** todo contato que chama a igreja e atendido, cadastrado e roteado pelo agente orquestrador, com handoff para atendimento humano quando necessario.
- **Fila de trabalho pastoral (Dashboard):** visitantes sem acompanhamento, atendimentos aguardando, relatorios pendentes e pendencias com prazo (conectar a celula em 24h, fonovisita), priorizados e acionaveis sem trocar de tela.
- **Ciclo ministerial G12 (Ganhar, Consolidar, Discipular, Enviar):** organiza a jornada da pessoa como uma maquina de estados unica, do primeiro contato a lideranca.
- **Relatorio de celula por WhatsApp:** o lider envia por texto/audio e o agente extrai e registra os dados.
- **Multi-tenant desde a primeira linha:** cada igreja e um tenant isolado (RLS), com sua propria credencial de IA (BYO-LLM) e seus proprios dados.

**Modelo de negocio:** assinatura mensal por porte (ate 100 = R$199; 101-200 = R$299; acima de 201 = R$399) + setup unico de R$ 1.000,00, processada via Asaas. O custo de LLM fica fora do preco (credencial BYO da igreja).

**Plataforma:** painel web responsivo mobile-first + PWA (sem app nativo no MVP). O usuario final (visitante, membro, lider enviando relatorio) usa o WhatsApp como interface operacional principal; o painel e opcional na rotina comum e obrigatorio somente para concluir acoes sensiveis que exigem autenticacao web. Stack alvo do MVP em VPS unica (Next.js, FastAPI + LangGraph, worker de filas, Evolution API), com Clerk (auth), Supabase (dados + RLS), Brevo (e-mail), Google Calendar (eventos) e Asaas (pagamento).

**Direcao visual travada:** software web claro, modern-minimal + tech utilitario; base zinc/off-white quente, acento teal dessaturado unico, numeros tabulares, bordas hairline, densidade balanced.

---

## 2. Personas

1. **Pastor / Admin da Igreja** — responsavel maximo pela igreja no sistema. Enxerga pendencias pastorais, toma decisoes sensiveis, acompanha celulas, lideres e relatorios e configura o sistema (WhatsApp, Agente, Assinatura, Equipe, Permissoes, Gerentes de Sistema).
2. **Lider de Celula** — opera a rotina semanal: envia relatorio pelo WhatsApp, acompanha membros e visitantes da sua celula e recebe alertas sobre seus liderados. Nao acessa o inbox do WhatsApp da igreja nem as telas de configuracao administrativa.
3. **Usuario final via WhatsApp (visitante, membro, lider)** — resolve a rotina pelo WhatsApp; o painel e opcional no uso comum e exigido apenas quando uma acao sensivel precisar de autenticacao web.
4. **Equipe de Consolidacao** — ministerio responsavel por acompanhar quem decide por Jesus e conectar visitantes sem celula; acessa o Dashboard de Consolidacao (acesso restrito).
5. **Admin do Sistema (Super-Admin / operador do SaaS)** — controla o negocio do SaaS: gerencia igrejas (tenants), verifica pagamentos e provisiona novas igrejas. Atua numa superficie separada do painel operacional das igrejas.

> Modelo de pessoa unificado (Design Lock — F6/F1): Conhecendo, Visitante, Discipulo, Lider e Pastor sao **estados da mesma pessoa**, com isolamento por `igreja_id` desde o primeiro registro.

---

## 3. User Stories

> Mantidas integralmente do `stories-requisitos20260611_163530.md` (aprovado). Nenhum requisito aprovado foi removido. As stories continuam sendo a fonte de verdade de comportamento; o Design Lock e a fonte de verdade de telas/navegacao/UI.

### Dominio: Autenticacao e Multi-tenant
- **US-01 — Login no painel.** Como Pastor/Admin, quero fazer login via Clerk para acessar o painel da minha igreja com seguranca.
- **US-02 — Isolamento de dados por igreja (multi-tenant).** Como Pastor/Admin, quero ver e operar somente os dados da minha igreja (RLS Supabase).
- **US-03 — Gestao de usuarios da igreja.** Como Pastor/Admin, quero convidar e gerenciar usuarios (lideres), com convite por e-mail (Brevo).
- **US-04 — Controle de acesso por papel.** Como Pastor/Admin, quero que cada papel veja apenas o que lhe compete (hierarquia "quem esta acima ve o que esta abaixo").

### Dominio: Conexao WhatsApp
- **US-05 — Conectar o numero oficial da igreja** via QR code (Evolution API), com estados de conexao.
- **US-06 — Monitorar e reconectar o WhatsApp**, sem perder mensagens.
- **US-07 — Nao registrar conversas pessoais do pastor** (apenas o numero oficial).

### Dominio: Atendimento por IA (Agente Orquestrador)
- **US-08 — Atendimento automatico de quem chama a igreja.**
- **US-09 — Coleta de dados e criacao de contato pelo agente.**
- **US-10 — Onboarding de contato/visitante pelo agente** (fluxo configuravel; notifica a consolidacao).
  - **delta (Onda 1 / #1):** quem fala pela 1a vez no WhatsApp nasce como `subetapa=novo_contato` ("contato"). A transicao contato → **visitante** NAO e decidida pela conversa (a pessoa dizer que foi a igreja nao basta): acontece por **evento real** — cadastro pelo lider, encaminhamento da equipe de consolidacao, ou **check-in na igreja** (QR que abre conversa com o orquestrador; pessoa nova no check-in entra ja como visitante). Contatos sem vinculo ministerial (empresa, outra cidade, vendas) sao marcados **CSIM** (`pessoas.sem_interesse=true` + motivo) e ficam **fora do funil pastoral**. *(Check-in/QR + lookup por CPF/e-mail = frente nova, ainda a especificar no PRD.)*
- **US-11 — Lista de conversas (inbox)** — acesso restrito a Pastor/Admin e usuarios liberados ao atendimento humano; lideres de celula NAO acessam.

### Dominio: Atendimento Humano (Handoff)
- **US-12 — Assumir atendimento (pausar IA).**
- **US-13 — Devolver atendimento para a IA.**
- **US-14 — Fila de atendimentos humanos aguardando.**

### Dominio: Painel / Fila de Trabalho Pastoral
- **US-15 — Dashboard de pendencias pastorais** (apenas tarefas a fazer, priorizadas).
- **US-16 — Acoes diretas na fila de trabalho** (assumir, resolver, atribuir, devolver).
- **US-17 — Proximas acoes por responsavel.**

### Dominio: Gestao de Visitantes e Contatos
- **US-18 — Lista de visitantes sem acompanhamento.**
- **US-19 — Detalhe do contato.**
- **US-20 — Conectar visitante a uma celula.**

### Dominio: Celulas e Lideres
- **US-21 — Cadastro de celulas.**
- **US-22 — Membros e visitantes de uma celula.**
- **US-23 — Alertas sobre liderados** (gatilhos configuraveis).

### Dominio: Relatorio de Celula via WhatsApp
- **US-24 — Enviar relatorio de celula pelo WhatsApp.**
- **US-25 — Visualizar relatorios de celula no painel.**
- **US-26 — Relatorio pendente vira acao na fila.**

### Dominio: Configuracao de Agentes IA e Credencial LLM
- **US-27 — Cadastrar credencial OpenAI (BYO)** — uma credencial propria por igreja; outros provedores ficam fora do produto (revisado pelo delta-052).
- **US-28 — Configurar comportamento do agente.**
- **US-29 — Configurar crons e agendamentos do agente.**

### Dominio: Calendario e Eventos
- **US-30 — Gerir eventos no calendario** (sincronizado ao Google Calendar).

### Dominio: Comunicados, Consentimento e Opt-out
- **US-31 — Registrar consentimentos separados por finalidade.** O atendimento solicitado, o cuidado pastoral, as tarefas operacionais e os comunicados possuem eventos independentes; iniciar uma conversa nao libera automaticamente as demais finalidades. *(revisado pelo delta-052)*
- **US-32 — Opt-out de comunicacao.**
- **US-33 — Envio segmentado de comunicados.**

### Dominio: Monetizacao e Assinatura
- **US-34 — Contratar assinatura com setup fee** (Asaas; PIX/boleto/cartao).
- **US-35 — Acompanhar status da assinatura.**
- **US-36 — Upgrade automatico de plano por porte.**

### Dominio: Consolidacao
- **US-37 — Lancar decisao por Jesus e iniciar consolidacao.**
- **US-38 — Dashboard de Consolidacao (acesso restrito).**
- **US-39 — Acompanhar etapas e concluir a consolidacao (Universidade da Vida).**
- **US-40 — Pendencias de consolidacao (conexao a celula e fonovisita).**

### Dominio: Assistente do Sistema (no painel)
- **US-41 — Assistente geral do sistema no painel** (ciente do papel e do tenant).

### Dominio: Administracao da Plataforma (Super-Admin)
- **US-42 — Gerir igrejas (tenants) do SaaS.**
- **US-43 — Provisionar nova igreja.**

> Cobertura de design: US-01..US-43 rastreadas no `design-contract.json`. US-42/US-43 estao representadas por telas de stub para rastreabilidade, em superficie separada (ver "Conflitos resolvidos" e Escopo Negativo).

> 🚧 **Onda 1 — em construcao (2026-06-15).** O console Super-Admin saiu do estagio de stub: implementacao incremental, fora do painel operacional. **Fatia 1 (backend) entregue:** migration `0010_platform_admins.sql` (allowlist de plataforma, sem `igreja_id`, fora do RLS por tenant), gate `get_platform_admin` (paralelo ao `get_current_user`, sem `set_tenant_context` → roda como service role/BYPASSRLS) e router `/admin/igrejas` (listar todas as igrejas · provisionar igreja + admin inicial via convite Brevo · alterar status/plano). **Pendente (Fatia 2+):** superficie propria `admin.igreja12.com.br` (frontend) + DNS, e o seed do primeiro `platform_admin`.

---

## 4. Requisitos Funcionais

> Mantidos do documento aprovado (RF-01 a RF-49). Cada RF rastreia as user stories de origem.

### Autenticacao e Multi-tenant
- **RF-01** — Autenticar usuarios via Clerk e estabelecer sessao. *(US-01)*
- **RF-02** — Isolar todos os dados por igreja (tenant) via RLS do Supabase. *(US-02)*
- **RF-03** — Convidar usuarios informando e-mail e papel, com e-mail de ativacao via Brevo. *(US-03)*
- **RF-04** — Listar e revogar acesso de usuarios da igreja. *(US-03)*
- **RF-05** — Aplicar controle de acesso por papel, restringindo telas, dados e acoes. *(US-04, US-22)*

### Conexao WhatsApp
- **RF-06** — Gerar QR code via Evolution API e exibir o estado da conexao. *(US-05)*
- **RF-07** — Permitir apenas um numero oficial conectado por igreja por vez. *(US-05)*
- **RF-08** — Monitorar o status da conexao e permitir reconexao. *(US-06)*
- **RF-09** — Registrar e atuar exclusivamente sobre mensagens do numero oficial. *(US-07)*

### Atendimento por IA
- **RF-10** — Rotear toda mensagem do numero oficial pelo agente orquestrador. *(US-08)*
- **RF-11** — Gerar respostas via provedor LLM configurado pela igreja. *(US-08, US-27)*
- **RF-12** — Criar/atualizar contato a partir das conversas (nome, telefone, origem, data). *(US-09)*
- **RF-13** — Executar fluxo de onboarding e registrar novos visitantes. *(US-10)*
- **RF-14** — Disponibilizar inbox e historico cronologico de cada conversa. *(US-11)*

### Atendimento Humano
- **RF-15** — Assumir uma conversa, pausando a IA, registrando responsavel e horario. *(US-12)*
- **RF-16** — Devolver a conversa para a IA, registrando o encerramento. *(US-13)*
- **RF-17** — Manter fila de atendimentos humanos com tempo de espera e acao de assumir. *(US-14)*

### Painel / Fila de Trabalho Pastoral
- **RF-18** — Exibir dashboard com visitantes sem acompanhamento, atendimentos aguardando e relatorios pendentes, ordenados por prioridade. *(US-15)*
- **RF-19** — Permitir acoes diretas: assumir, resolver, atribuir e devolver para IA. *(US-16)*
- **RF-20** — Agrupar proximas acoes por responsavel, com grupo "Nao atribuido". *(US-17)*

### Visitantes e Contatos
- **RF-21** — Listar visitantes sem acompanhamento, ordenaveis pelo tempo desde o 1o contato. *(US-18)*
- **RF-22** — Exibir detalhe do contato com dados, celula, status e acesso a conversa. *(US-19)*
- **RF-23** — Vincular contato a celula valida, encerrando "em acompanhamento" e marcando "membro". *(US-20)*

### Celulas e Lideres
- **RF-24** — Criar, editar e inativar celulas (no minimo nome e lider responsavel). *(US-21)*
- **RF-25** — Exibir detalhe da celula com membros e visitantes. *(US-22)*
- **RF-26** — Gerar alertas ao Lider por gatilhos configuraveis, marcando como "tratado" ao baixar. *(US-23)*

### Relatorio de Celula
- **RF-27** — Coletar via WhatsApp os dados minimos do relatorio, com confirmacao antes de registrar. *(US-24)*
- **RF-28** — Listar relatorios por celula e data e exibir o detalhe completo. *(US-25)*
- **RF-29** — Identificar celulas sem relatorio e gerar pendencia, baixada ao receber o relatorio. *(US-26)*

### Configuracao de Agentes IA e Credencial LLM
- **RF-30** — Cadastrar credencial LLM, armazenando-a protegida e validando antes de ativar. *(US-27)*
- **RF-31** — Modelar a credencial OpenAI BYO configuravel e isolada por igreja. *(US-27, delta-052)*
- **RF-32** — Configurar instrucoes/comportamento do agente, com efeito nas conversas seguintes. *(US-28)*
- **RF-33** — Criar, editar e desativar agendamentos (crons) de acoes do agente. *(US-29)*

### Calendario e Eventos
- **RF-34** — Criar eventos (titulo, data/hora, descricao) e sincronizar com o Google Calendar. *(US-30)*
- **RF-35** — Exibir eventos em calendario com visoes por mes, semana e dia. *(US-30)* — **revisado (delta-049): abas Semana/Mes/Ano/A confirmar/Planejamento; ver `docs/design/AGENDA-EVENTOS-EVT0-decisao.md`.**

### Comunicados, Consentimento e Opt-out
- **RF-36** — Registrar e exibir consentimento versionado por finalidade. A primeira mensagem pode registrar somente `atendimento_solicitado`; `cuidado_pastoral`, `tarefas_operacionais` e `comunicados` exigem seus proprios eventos de concessao ou retirada. *(US-31, delta-052)*
- **RF-37** — Permitir opt-out pelo WhatsApp e refletir no contato. *(US-32)*
- **RF-38** — Enviar comunicados segmentados pelo numero oficial, excluindo opt-out ou ausencia de consentimento para `comunicados`. *(US-33, delta-052)*

### Monetizacao e Assinatura
- **RF-39** — Exibir os tres planos por porte e processar contratacao via Asaas, com setup de R$ 1.000,00. *(US-34)*
- **RF-40** — Ativar a assinatura apos confirmacao do pagamento. *(US-34)*
- **RF-41** — Exibir status da assinatura (plano, valor, situacao, proxima cobranca). *(US-35)*
- **RF-42** — Contar o porte por pessoas cadastradas e promover ao plano seguinte automaticamente. *(US-36)*

### Consolidacao
- **RF-43** — Lancar decisao por Jesus e iniciar consolidacao nos dois casos (pessoa em celula / visitante sem vinculo com prazo 24h). *(US-37)*
- **RF-44** — Dashboard de Consolidacao restrito, com dados, celula, etapa, filtro e destaque de prazos. *(US-38)*
- **RF-45** — Registrar etapas e concluir a consolidacao manualmente; ao concluir, marcar apto a proxima turma da CD e remover do dashboard, sem matricula automatica. *(US-39)*
- **RF-46** — Gerar pendencias de consolidacao na fila ("Conectar a celula" 24h e "fonovisita"), alertar por WhatsApp e baixar ao concluir. *(US-40)*

### Assistente do Sistema
- **RF-47** — Assistente de IA no painel, ciente do usuario logado e suas permissoes, restrito ao tenant. *(US-41)*

### Administracao da Plataforma (Super-Admin)
- **RF-48** — Painel de Super-Admin para gerir todas as igrejas (tenants), separado do painel operacional. *(US-42)*
- **RF-49** — Verificar pagamento e provisionar nova igreja como tenant isolado. *(US-43)*

> **Requisitos funcionais derivados do Design Lock** (refinamentos rastreados as stories aprovadas, nao novos escopos):
> - **RF-D1** — RBAC por papeis **acumulados**: cada pessoa carrega um conjunto de papeis no cadastro; menu e dashboard sao a uniao dos acessos. Itens de Configuracao so aparecem para o papel `admin`. *(US-03, US-04 — delta-005/011/032)*
> - **RF-D2** — Tela **Permissoes** (admin): matriz papel x tela, fonte de verdade dos acessos, refletindo no menu/dashboard em tempo real. *(US-04 — delta-010)*
> - **RF-D3** — Promocao automatica de etapa/papel pela **trilha ministerial** (state machine unica), sem edicao manual no fluxo normal; admin edita apenas como excecao. *(US-04, US-18 — delta-013/014/031)*
> - **RF-D4** — "Resolver"/"Atribuir" na fila aciona compositor que envia mensagem pelo WhatsApp oficial, prefixada "Nome [papel]: mensagem", com ranking de lideres aptos (mesmo genero obrigatorio, faixa etaria, regiao). *(US-16, US-17 — delta-006/025)*
> - **RF-D5** — Comunicados aceitam multiplos segmentos e agendamento cron (once/daily/weekly/biweekly/monthly). *(US-33 — delta-009)*
> - **RF-D6** — Captura de relatorio de celula por texto ou audio, com extracao de presentes, visitantes, decisoes e oferta; decisao por Jesus abre consolidacao automaticamente. *(US-24 — delta-041)*
> - **RF-D7** — Autorizacao real no backend (revalidacao de `igreja_id` + papel em cada endpoint); o agente usa as mesmas funcoes e validacoes de um humano. *(US-02, US-04 — F4/F5/delta-033/034)*

---

## 5. Requisitos Nao-Funcionais

### Seguranca
- **RNF-01** — Autenticacao exclusivamente via Clerk; nao armazenar senhas proprias.
- **RNF-02** — Isolamento multi-tenant garantido em nivel de banco por RLS do Supabase.
- **RNF-03** — Credenciais LLM e chaves de integracao cifradas e nunca exibidas em texto claro.
- **RNF-04** — Todo trafego sobre HTTPS (TLS), com certificado automatico.
- **RNF-05** — Controle de acesso por papel respeitado em todas as requisicoes, inclusive no backend.
- **RNF-06** — Registro de consentimento e suporte a opt-out por contato (boa pratica LGPD; dados religiosos/sensiveis). Modelar versao do termo, data/hora de aceite, re-aceite em nova versao e mascara de CPF/dados sensiveis nos logs (fundacao A2/delta-040).

### Performance
- **RNF-07** — Dashboard carrega a visao inicial em ate 3s em 4G tipico.
- **RNF-08** — Mensagens do numero oficial processadas/roteadas em ate 5s do webhook (excluindo tempo do LLM).
- **RNF-09** — Listas com paginacao respondendo em ate 2s com ate 1.000 registros.

### Usabilidade
- **RNF-10** — Painel web responsivo mobile-first (celular e desktop).
- **RNF-11** — Painel instalavel como PWA (sem loja de apps).
- **RNF-12** — Priorizacao hierarquica da informacao (urgente primeiro), coerente com fila de trabalho.
- **RNF-13** — Usuario final resolve a rotina comum pelo WhatsApp, sem instalar nada. O painel permanece opcional para uso cotidiano e obrigatorio somente quando uma acao sensivel exigir autenticacao web. *(revisado pelo delta-052)*
- **RNF-14** — Toda a interface do painel em portugues brasileiro.

### Confiabilidade
- **RNF-15** — Processos sempre-ligados (LangGraph e Evolution API) em modo persistente, com restart automatico apos falha.
- **RNF-16** — Mensagens recebidas durante indisponibilidade nao geram contatos duplicados apos reconexao.
- **RNF-17** — Processamento de webhooks via worker de filas, com reprocessamento em falha temporaria.

### Restricoes de plataforma e custo (MVP)
- **RNF-18** — Stack do MVP (Next.js, FastAPI + LangGraph, worker, Evolution API) em VPS unica com >= 4GB RAM.
- **RNF-19** — Sem app nativo (iOS/Android) no MVP; mobile via web responsivo + PWA.
- **RNF-20** — Custo de LLM fora do preco do PastorAI (BYO da igreja).

### Auditoria e fundacoes (Design Lock — NAO-UI)
- **RNF-21** — Multi-tenant desde a 1a linha: toda tabela nasce com `igreja_id` e RLS; igreja piloto e o 1o registro. (F1/delta-030)
- **RNF-22** — Maquina de estados central de maturidade da pessoa (Conhecendo -> ... -> Lider/G12) como campo de estado governado por regras. (F2/delta-031)
- **RNF-23** — Infra de crons com gatilhos por estado do banco (prazo vencendo, meta atingida), nao so horario fixo; base do motor de SLAs. (F9/A1/delta-038/039)
- **RNF-24** — Logs de conversacao do agente e consumo de IA por igreja (modelo, tokens, custo) desde o dia 1. (F8/delta-037)
- **RNF-25** — Relacao lider->liderado como campo de lideranca no cadastro da pessoa (integridade do organograma G12). (F7/delta-036)

---

## 6. Metricas de Sucesso

**Produto / Operacao pastoral**
- % de visitantes conectados a uma celula dentro de 24h (pendencia "Conectar a celula" baixada no prazo).
- Tempo medio para baixar uma pendencia da fila pastoral (assumir -> resolver).
- % de decisoes por Jesus que entram em consolidacao individual.
- % de celulas com relatorio recebido na semana esperada.
- Tempo medio de resposta no atendimento humano (espera na fila de handoff).
- Reducao de contatos sem registro (toda conversa com o numero oficial gera contato).

**IA / Atendimento**
- % de mensagens atendidas automaticamente pela IA sem handoff.
- Latencia de roteamento do webhook (alvo: <= 5s, RNF-08), monitorada continuamente.
- Custo de IA por igreja (tokens/modelo) por periodo — base do BYO-LLM e da depuracao.

**Plataforma / Negocio**
- Disponibilidade da conexao WhatsApp por igreja (% de tempo online).
- Tempo de carga do dashboard (alvo <= 3s em 4G, RNF-07).
- Conversao de checkout (planos exibidos -> assinatura ativa apos pagamento).
- Taxa de inadimplencia das assinaturas e tempo de provisionamento de nova igreja.
- Numero de igrejas (tenants) ativas e crescimento.

---

## 7. Escopo Negativo

Itens deliberadamente **fora do MVP** (nao implementar, mesmo quando ha rastreabilidade ou stub de UI):

- **Console Super-Admin operacional (US-42/US-43)** — gestao de tenants e provisionamento sao **superficie separada** do painel da igreja; no `design-contract.json` existem apenas telas de **stub** (`super-admin-igrejas`, `super-admin-provisionar`) para rastreabilidade, a serem prototipadas em artifact proprio (delta-002/024). Nao construir no painel operacional.
- **Universidade da Vida e Capacitacao Destino — telas BLOQUEADAS no MVP** (estado `locked-em-breve`, rotulo "em breve"). O conteudo detalhado de aulas/modulos e o modelo de 6 livros da CD ficam para roadmap (delta-019/028).
- **App nativo iOS/Android** — coberto por web responsivo + PWA (RNF-19).
- **RBAC hierarquico completo G12** alem dos papeis ativos do MVP (Pastor/Admin, Lider de Celula, usuario autorizado ao atendimento humano) — hierarquia completa (membro, lider de ministerio, niveis G12) e roadmap.
- **Portal do Membro** (dashboard pessoal da celula, bate-papo, solicitar conversa com pastor/lider).
- **Trilhas de formacao completas** (UV e CD com acompanhamento de etapas).
- **Aba de Gestao de Celula do Lider** (materiais, planner, metas, planejamento de multiplicacao) e **agente de IA dedicado a gestao da celula**.
- **Ministerios e lideres de ministerio.**
- **Provedores LLM alem de OpenAI, incluindo OpenRouter** — modelagem historicamente extensivel, porem o produto aprovado usa somente OpenAI BYO por igreja.
- **Formalizacao LGPD completa** alem do registro de consentimento/opt-out e mascaramento de dados sensiveis (estrutura preparada, formalizacao plena e futura).
- **Dados reais da igreja piloto na fase de design** — amostras representativas validam fluxo/estados, nao representam producao (delta-003).

> Observacao de rastreabilidade: esta secao registra os cortes do MVP original. Universidade da Vida, Capacitacao Destino e os especialistas de dominio permanecem na visao futura pelo delta-052; isso nao significa que ja estejam implementados. UV e CD estao excluidas da missao corrente e dependem de PRDs e missoes proprias.
>
> As telas legadas `contatos`, `celulas` e `relatorios` permanecem validas para deep-link, mas saem do menu — cobertas por `ganhar`, `central-celula` e pelos estagios do ciclo G12 (delta-012). Isso e reorganizacao de navegacao, nao remocao de requisito.

### Deltas #10b — Redesenho do agente (2026-06-24)

> Mudancas estruturais decididas com o usuario no redesenho do agente orquestrador (Onda 5 / #10b) que **revisam decisoes deste PRD**. Cada fase do redesenho anota seu proprio delta ao ser executada (regra de trabalho #2). Plano completo: `plano-redesenho-agente.html`.

- **delta-043 — Configuracao do agente e do MASTER (revisa US-28/RF-32).** O PRD original previa o **admin local** configurando o comportamento/instrucoes do agente (US-28, RF-32). Decisao do usuario (2026-06-24): a configuracao do agente orquestrador passa a ser **exclusiva do master** (`platform_admin`); o admin da igreja **nao edita** — apenas **alimenta a base de conhecimento** e **solicita** mudancas ao master (fila de requisicao). A igreja **nasce herdando** o "agente geral" do master (ja ha `platform_orchestrator` -> `_seed_agent_from_template`) com opcao de **reinicializar pelo padrao**. Implica: travar `PUT /agent/config` para admin (hoje liberado), `reset-agent` pelo master e tabela `agent_config_requests`.
- **delta-044 — Design Lock: nova superficie "Base de Conhecimento" + requisicao (revisa o Design Lock de 2026-06-12).** O redesenho adiciona ao `#agente` (admin) uma **aba "Base de Conhecimento"** (upload de arquivos/audio, marcar conversa, status de ingestao) e uma **UI de requisicao** admin->master. Sao telas/abas **fora do contrato travado** (13/13 regras, 18 telas) -> desvio formal autorizado por este delta. O delta-052 restringe "marcar conversa" a criar uma pendencia ou rascunho privado para revisao: a conversa bruta nunca entra na base institucional, e somente um documento redigido, revisado e publicado por admin autorizado pode virar conhecimento oficial. As demais telas do Design Lock permanecem inalteradas.
- **delta-045 — Isolamento por igreja no caminho assincrono (worker WhatsApp).** Implementado na Fase 0 (#10b): `current_igreja_id()` passa a honrar tambem o GUC `app.tenant_igreja_id` (migration `20260624_003030`), e o worker injeta o tenant-context por `igreja_id` (`set_tenant_context_for_igreja`) apos resolver a igreja pelo `instance`, caindo no papel `authenticated` para a RLS valer fora do HTTP. Reforca RNF-02/RNF-21 (o worker rodava como `postgres`/BYPASSRLS, isolando so por `WHERE igreja_id` manual). Pre-requisito de seguranca para KB/tools no WhatsApp.
- **delta-046 — Autorizacao por interlocutor no WhatsApp (estende RF-D7).** Implementado na Fase 2 (#10b): o agente resolve o privilegio de QUEM fala (`PrivilegeContext`: papel via `app_users.pessoa_id`->`user_roles`, celulas lideradas via `celulas.lider_id`, tipo, CSIM) e so executa acoes ministeriais (registrar_decisao/marcar_presenca/vincular_celula/avancar_trilha) para lider/pastor/admin. Gate no roteamento (relatorio so de ministerial) + no executor de tools (nega + audita `tool_negada`), determinístico — o LLM nunca decide autoridade. Fecha o escalonamento (um contato registrava decisao para si via "relatorio" falso). Sem migration.
- **delta-047 — Higiene de seguranca do agente (decisao "varios agentes": orquestrador unico vence; finanças = ilustrativo por ora).** (a) **Opt-out persistido (refina US-32/RNF-06):** o agente passa a respeitar `pessoa.optout` no inbound — quem pediu sair NAO recebe auto-resposta (mensagem fica no inbox p/ humano); antes so checava a mensagem atual. Re-opt-in e manual pelo humano (follow-up). (b) **Whitelist de args nas tools** (`TOOL_ARG_SCHEMA`): o executor rejeita args fora do schema antes do `**args` + except defensivo. (c) **Hardening anti prompt-injection** no refino: msg do usuario tratada como dado, nao instrucao (pedaco adiantado dos guardrails / RNF-06). Decidido: SEM registry de personas e SEM loop multi-agente (precisaria de memoria duravel ausente); vertical financeiro so com demanda real (pre-req: vinculo VERIFICADO pessoa<->app_user, que a Fase 2 ainda nao tem — hoje o vinculo e por telefone/nao-verificado, aceitavel p/ relatorio mas NAO p/ dinheiro). Sem migration.
- **delta-048 — Fila de requisicao admin->master implementada (concretiza delta-043/044; fecha #10b Fase 1).** Tabela tenant `agent_config_requests` (migration `20260624_171110`, RLS por `current_igreja_id()`). Admin (tenant): `POST/GET /agent/config/requests` — abre requisicao por mensagem livre e ve o historico/status (substitui o "fale com o suporte" da aba Comportamento, read-only). Master (console): `GET /admin/igrejas/{id}/agente/requests` + `POST /admin/agente/requests/{id}/resolver` (status `atendida|recusada` + resposta; 409 se ja resolvida; auditado `agente_requisicao_resolver`); o master ajusta a config pelo editor existente e marca a requisicao — NAO ha auto-aplicacao (master segue autor real da config, delta-043). Status: `pendente|atendida|recusada`. UI da requisicao surge no `#agente` (admin) e por-igreja na aba Agente do console (master). Inbox global/notificacao = follow-up (nao construido). Junto: `reset-agent` pelo master (PR #33) ja entregue. Com isso a Fase 1 da governanca do agente esta completa.
- **Pendentes registrados em 2026-06-24 (historico):** KB/RAG com pgvector (revisa "dados so via tools"); gatilho proativo "±1h da celula" + agenda normalizada (sobre RNF-23); checkpointer duravel do grafo; multiprovedor; guardrails de entrada/saida (sobre RNF-06); vertical financeiro. O delta-052 substitui a opcao multiprovedor por OpenAI BYO e consolida o escopo atual sem tratar esta lista historica como implementacao.

---

### Deltas — Agenda de Eventos (Onda 2 / EVT-0, 2026-06-29)

> Mudancas estruturais decididas com o usuario apos a auditoria E0 (base `baeb26c`) que **revisam decisoes deste PRD** sobre Calendario e Eventos. Fonte unica da decisao: `docs/design/AGENDA-EVENTOS-EVT0-decisao.md`. Cada PR da implementacao (EVT-1..7) anota sua propria evidencia ao ser executada (regra de trabalho #2).

- **delta-049 — Agenda vira modulo de 5 abas (revisa RF-34/RF-35 e a entidade `evento`).** O PRD previa visoes **mes/semana/dia** (RF-35) e um `evento` minimo (`titulo/data/hora/descricao/google_event_id`). Decisao do usuario (2026-06-29): honrar a **spec nova** "Modulo Agenda de Eventos v1.0" — abas **Semana/Mes/Ano/A confirmar/Planejamento**, com **status** (`confirmado`/`a_confirmar`), **recorrencia por dia da semana** (visao Semana; Mes/Ano por data), **tipo/cor** do evento e campos de confirmacao (**publico/antecedencia/mensagem**). Reuso: **estender a tabela `events`** existente (nao criar tabela paralela). **MVP = EVT-1..5** (CRUD manual + Semana/Mes/Ano + criar/editar/detalhe + aba "A confirmar" + confirmacao manual **sem envio**). Evento manual nasce `confirmado`; importado nasce `a_confirmar`. As abas administrativas ("A confirmar"/"Planejamento") sao so de Pastor/Admin.
- **delta-050 — Google passa a IMPORT (Google→app como "a confirmar"), fora do MVP; corrige risco de token global (revisa RF-34).** A spec quer **importar** eventos do Google (entram pendentes, so comunicados apos confirmacao), enquanto o codigo atual faz **PUSH** (app→Google) usando **token GLOBAL de `settings`** (Legacy) desconectado do OAuth por igreja (`calendar_sync`) — risco de **cruzamento entre tenants**. Decisao: **nao implementar nada de Google antes de corrigir o token por igreja**; o import e o **EVT-6**, que comeca obrigatoriamente por esse fix. O MVP (EVT-1..5) e 100% manual e nao toca Google.
- **delta-051 — Papeis e gate de criacao da Agenda (refina RF-34).** "Pastor/Admin" da spec = papeis reais `admin` (implicito) + `pastor`: criam, editam, confirmam e configuram planejamento. "Lider e demais" (`lider_g12`, `lider_consol`, `lider_celula`, `lider_mult`, `operador`, `membro`) **so visualizam** e sao notificados. Hoje `POST /events` aceita `["pastor","lider_g12"]` (lider cria, diverge da spec) → no **EVT-2** o gate de criar/editar/confirmar passa a `require_role(["pastor"])` e **`lider_g12` sai**. Envio real de notificacao so a partir do **EVT-7** (outbound guard B2 mantido ate la).

### Delta — Produto WhatsApp-first e inteligencia por igreja (2026-08-27)

- **delta-052 — WhatsApp como interface principal, memoria privada e especialistas globais por dominio.** Esta decisao amplia o roadmap para a conclusao do produto e nao afirma implementacao. O WhatsApp oficial passa a ser a interface operacional principal para membros e liderancas; o painel web concentra configuracao inicial, governanca, supervisao, excecoes e conclusao autenticada de acoes sensiveis. Uma unica definicao global e versionada do LangGraph atende todas as igrejas, sempre com contexto de tenant criado no servidor e isolamento de dados, memoria, documentos, credenciais e execucao por `igreja_id`. O orquestrador coordena especialistas comuns de forma incremental. Atendimento, Central de Celulas, Agenda e Consolidacao integram a missao atual; Universidade da Vida e Capacitacao Destino permanecem na visao futura e exigem PRDs e missoes proprias. Especialistas nunca enviam mensagens diretamente. Dados estruturados do sistema e documentos aprovados, versionados e com audiencia controlada formam o conhecimento oficial. Conversas, midias, transcricoes, resumos e checkpoints formam memoria privada e nunca viram conhecimento institucional automaticamente. Quando faltar informacao oficial, o agente declara a lacuna e encaminha a pendencia ao responsavel configurado para o setor. Consentimentos passam a ser independentes para `atendimento_solicitado`, `cuidado_pastoral`, `tarefas_operacionais` e `comunicados`, com opt-out global prevalecendo. O historico privado e retido ate solicitacao da pessoa pelo WhatsApp e aprovacao do admin no painel; a exclusao deve alcançar mensagens, midias, transcricoes, resumos, checkpoints e vetores derivados, preservando apenas auditoria minima sem conteudo pessoal. Acoes operacionais comuns exigem resumo e confirmacao explicita no WhatsApp; permissoes, vinculos de terceiros, dados pastorais restritos, exclusoes, financas, publicacao de conhecimento e configuracao da igreja exigem o painel. A primeira vertical completa sera o relatorio de celula, com lembrete, entrada por texto ou audio, resumo editavel, confirmacao, gravacao no dominio canonico e comprovante apos commit. O provedor do produto permanece **OpenAI BYO por igreja**; OpenRouter nao integra o PastorAI.

- **delta-053 — rascunhos de governanca preparados pelo Admin Master no Console.** O Admin Master autenticado pode preparar para cada igreja os quatro rascunhos de finalidade do contrato D2B2b2. Tenant e ator sao derivados no backend; e-mail nao e regra de autorizacao nem configuracao versionada. Essa superficie e draft-only: nao permite ao Master escolher hipotese juridica, declarar que a operacao depende de consentimento, decidir politica de menores, atestar, aprovar, representar dono factual, encarregado, juridico ou controlador, nem preencher registros nominais. A UI mantem `DRAFT_NOT_APPROVED` visivel e nao oferece transicao, publicacao ou ativacao. A fatia D2B2b3A integrada inclui migration versionada, persistencia, API e painel do Console Master, com revisao otimista e auditoria sem payload; a migration foi testada em PostgreSQL 17 descartavel. Esta missao nao aplicou a migration D2B2b3A; DEV e PROD confirmaram a ausencia. Painel do tenant, aprovacoes, catalogo, evidence store, writer, WhatsApp, agente, deploy manual ou do backend e D2C continuam bloqueados.

  No baseline `15deaf88fd4cab5b4bebdd1435a81c8b33c2b159`, o preflight PROD somente leitura confirmou `DATABASE_URL` presente e `M06_MIGRATION_DATABASE_URL` ausente. `current_user` e `session_user` convergiram para a mesma identidade sanitizada; a role runtime possui `NOSUPERUSER`, `BYPASSRLS`, `LOGIN` e `INHERIT`, e owner de `public.igrejas` e `public.app_users` e possui `SELECT` e `REFERENCES` efetivos nessas tabelas-pai. A tabela alvo D2B2b3A, o validator e a propria `public.schema_migrations` estavam ausentes. A flag `PURPOSE_CONSENT_GOVERNANCE_DRAFTS_ENABLED` permaneceu `false`. Esta missao nao aplicou a migration D2B2b3A; DEV e PROD confirmaram a ausencia. A PR #321 integrou a reconciliacao documental anterior no merge `15deaf88fd4cab5b4bebdd1435a81c8b33c2b159`; esse merge gerou o deployment automatico Vercel frontend Production `6141449639`, com `SUCCESS`, em 2026-08-28T12:53:35Z. Essa metadata prova somente o frontend, sem provar backend, banco ou Supabase. O preflight VPS em si nao executou deploy manual ou do backend, migration, restart ou alteracao da flag. A leitura comprova identidade, ownership e ACL do caminho runtime atual, mas nao o comportamento da tabela futura sob `FORCE RLS`; o caminho de migration permanece bloqueado pela ausencia de `M06_MIGRATION_DATABASE_URL` e do ledger publico.

  **delta-054 — bootstrap do ledger publico integrado e comprovado somente offline, ainda nao aplicado.** Sobre a base versionada `b43ad92028374fa6763ef10f5eb7a379afd3e7a2`, a implementacao adicionou o subcomando fail-closed `bootstrap-ledger`, separado de `harden-ledger`, com confirmacao literal `BOOTSTRAP_LEDGER` e destino somente por `M06_MIGRATION_DATABASE_URL`. Em PostgreSQL 17 ele cria atomicamente apenas o ledger vazio `public.schema_migrations`, no contrato owner-only final com colunas, chave primaria e defaults exatos, RLS, policy deny e ACL minima. Objeto ou tipo homonimo, grants ou default privileges perigosos, membership, ownership ou forma fisica divergente abortam e revertem; a reaplicacao exata e um no-op. A implementacao offline passou em 42/42 testes unitarios, 87/87 em PostgreSQL 17-alpine descartavel em duas execucoes independentes e 87/87 em Supabase PG17 17.6.1.159 descartavel em duas execucoes independentes; a revisao de seguranca resultou em `GO`. A suite RLS completa, em execucao serial limpa no PostgreSQL 17 descartavel, passou em 326/326, com 3803 deselecionados e 2 warnings preexistentes, em 162.77s. A tentativa local da suite offline integral foi interrompida apos 5 min sem saida ou progresso; o resultado permanece `INCONCLUSIVO`, nao verde nem falha e nao foi reclassificado. Os workflows Backend Tests da PR #323 e do pos-merge concluiram com `SUCCESS`. O comando nao descobre o catalogo, nao le ou altera `supabase_migrations`, nao reconcilia, nao faz backfill e nao aplica ou registra migration. O ledger vazio preserva o bloqueio de `status` e `apply` ate existir prefixo integro do catalogo, humanamente reconciliado, com no maximo uma migration pendente.

  A PR #323, HEAD `74d3f2d87a7ffad501432b2d9fc4163bd3b4ada4`, foi integrada em `main` pelo merge `3a5789c784017ab15a43e28c4270d25af8618359` em `2026-08-28T15:24:58Z`; seus cinco workflows e os cinco pos-merge concluiram com `SUCCESS`. A Vercel registrou o Preview automatico frontend `6143773477`, com `SUCCESS`, em `2026-08-28T15:22:43Z`, e o Production automatico frontend `6143819601`, com `SUCCESS`, em `2026-08-28T15:25:43Z`. Essas metadatas provam somente o frontend, sem provar backend, banco ou runtime. O bootstrap esta integrado, mas nao aplicado. Nao houve deploy manual ou do backend, acesso aos bancos DEV ou PROD, bootstrap ou migration compartilhada, restart ou alteracao de credencial, flag, runtime, agente ou canario. O preflight PROD e o deployment automatico frontend da PR #321 permanecem evidencias historicas separadas.

  **delta-055 — pacote e verificador integrados, comprovados offline, decisoes humanas pendentes, nao aplicado.** O pacote deny-state versionado e o verificador stdlib separado do runner, desenvolvidos e comprovados offline sobre a base auditada `cfeba13c0a9d08288f8c956ee2f35ddc1c0c35b7`, foram integrados pela PR #325, HEAD `d9595c3958fec98a875d15de2b6647d6b1de435e`, no merge `ab7d09f07db96d5c63a2cc32dddf3f910e23bac2` em `2026-08-28T20:18:08Z`, conforme `docs/decisions/2026-08-28-migration-history-reconciliation-contract.md`. O estado e `INTEGRADO / COMPROVADO OFFLINE / DECISOES HUMANAS PENDENTES / NAO APLICADO`. A integracao nao acessou DEV ou PROD, nao materializou inventario de ambiente ou decisao humana e nao reconciliou nenhum ledger. O verificador nao acessa banco, rede, ambiente ou variaveis de ambiente, nao executa SQL, DML ou escrita, nao infere migration aplicada e nao cria, altera ou preenche ledger. Os ledgers nativo e publico permanecem independentes: `supabase_migrations.schema_migrations` e `public.schema_migrations` nao sao equivalentes. Qualquer sucesso estrutural conserva `OPERATIONAL_AUTHORIZATION=BLOCKED`.

  Os workflows da PR concluiram com `SUCCESS`: Backend `33207468055`, E2E `33207468044`, Frontend `33207468014`, RLS `33207468132` e Tooling `33207468082`. Os pos-merge tambem concluiram com `SUCCESS`: Backend `33207645381`, E2E `33207645348`, Frontend `33207645362`, RLS `33207645399` e Tooling `33207645340`. A Vercel registrou o Preview automatico frontend `6147914118`, com `SUCCESS`, em `2026-08-28T20:16:00Z` no HEAD, e o Production automatico frontend `6147952424`, com `SUCCESS`, em `2026-08-28T20:18:55Z` no merge. Essas metadatas provam somente o frontend, sem provar backend, banco ou runtime; nao houve deploy manual ou do backend, migration, bootstrap, hardening, restart, flag ou runtime nesta missao.

  **Evidencia offline:** `98/98` testes do verificador, `26/26` testes documentais e `42/42` testes offline do runner passaram, no agregado `166 passed/45 skipped`; os skips correspondem as integracoes puladas por ausencia deliberada de banco descartavel. O template deny-state terminou bloqueado com exit `8`.

  **delta-056, estado pre-merge historico do capturador e materializador.** Sobre a base de catalogo `656d1d9eebe90ad4b2cbb35c21939a6796c46bfe`, com 75 migrations e digest `84ddbdb1a858c46e4cd6086698d4738574293fa4b72e122e413557a608f9097f`, a matriz focal concluiu `166/166`, incluindo dois casos reais de PostgreSQL 17 em container descartavel dedicado. O SQL allowlisted tem SHA-256 `8b589e5dda722691fead34cbd63cab75a7a22f32e0cf4bdfe64d6cef603866ee`, e a revisao independente resultou em `GO`. Naquele snapshot, o capturador/materializador ainda era candidato, nao integrado, e nenhum inventario tinha sido capturado. O resultado preservado continua somente offline e nao autoriza operacao.

  **delta-057, inventarios DEV e PROD capturados, nao revisados e bloqueados.** O capturador/materializador foi integrado pela PR #327, HEAD `c4f7a25b81a8091a0d74783c816a168bb7adf44d`, no merge `f9201a06495fad138e313e4149ad9275ff896900`; a PR #328 integrou o hotfix, HEAD `2cbdfaf39ae11d984f0aa27dfcf0910c25984840`, no merge `04e5c1720bf89313718c4159a2ac9d0eeeed3c25`. Em PostgreSQL 17, DEV registrou 33 linhas no ledger publico e 6 no nativo em `2026-08-28T22:43:11.454382Z`; PROD registrou o ledger publico `ABSENT_CONFIRMED`, com 0 linhas, e 32 linhas no nativo em `2026-08-28T22:47:43.965243Z`. `native.name` permaneceu sempre `null`. Os dois pacotes estao em `EVIDENCE_CAPTURED_UNREVIEWED`; cada verifier terminou com exit `8`, `HUMAN_EVIDENCE_BLOCKED`, e a checagem conjunta terminou `CROSS_PACKAGE_OK`. A matriz focal offline pos-captura passou com `163 passed, 2 skipped` em `1.40s`; isso nao e suite integral nem reexecucao PostgreSQL. As saidas locais foram originalmente materializadas com modo `0600` e `O_EXCL`; depois do versionamento, a protecao dos seis artefatos depende da sanitizacao e da ACL do repositorio, nao do modo do checkout. A captura foi somente leitura e nao executou DML, runner, `bootstrap-ledger`, `harden-ledger`, `status`, `apply`, deploy, flag ou runtime; nao prova decisao humana, migration aplicada, prefixo reconciliado ou autorizacao operacional.

  **delta-058, artefatos integrados e versionados, inventarios ainda nao revisados e bloqueados.** A PR #329, HEAD `c5ae430aa865dbd6371953d43e4a4447ca8e6618`, integrou e versionou os seis artefatos no merge `341f38a7f1c6993c74d85e99748cb60046cd4501` em `2026-08-29T00:04:50Z`. Os cinco workflows da PR e os cinco pos-merge concluiram com `SUCCESS`. O merge gerou o deployment automatico Vercel frontend Production `6150482852`, com `SUCCESS`, em `2026-08-29T00:05:33Z`. Essa metadata prova somente o frontend, sem provar deploy manual ou do backend, banco ou runtime. A integracao versiona a evidencia sanitizada ja capturada, mas nao revisa os inventarios, nao altera `EVIDENCE_CAPTURED_UNREVIEWED` ou `HUMAN_EVIDENCE_BLOCKED`, nao aplica migration e nao libera o runner nem qualquer autorizacao operacional.

  **delta-059, revisao independente bloqueada concluida e proposta de remediacao offline.** `REVIEWER-01` classificou DEV como `BLOCKED_LEDGER_DIVERGENCE` e PROD como `BLOCKED_EVIDENCE_INSUFFICIENT`; o registro externo tem SHA-256 `18ec23b3634ae591e771c9df2e2b6d3c44f69f72e6e2bbd854fbb1fc0fb0b133`. `OWNER-01` aceitou o bloqueio, manteve `operational_authorization=false` e autorizou somente a preparacao offline no registro externo de SHA-256 `0c2e46025b2650eea089777d17cebe5c566fb3d6ed9b68b4f9a1b5e049c59240`. A decisao OWNER-01 registrada nao versiona registro bruto ou identidade. A proposta recomenda, para decisao humana futura, corte de epoca controlado somente apos atestacao completa do schema; nao autoriza backfill, migration, runner ou ambiente.

  **delta-060, manifesto de expectativas da fonte criado e ambientes nao atestados.** O manifesto estatico foi criado sobre a base `7f18f7e8b44cd50e6f6033867fb97bfa9eb9c9e6`, fixa 75 migrations e o digest `84ddbdb1a858c46e4cd6086698d4738574293fa4b72e122e413557a608f9097f`, e declara `SOURCE_LEVEL_EXPECTATION_ONLY`. O verificador terminou em `SCHEMA_EXPECTATION_MANIFEST_VERIFIED_SOURCE_ONLY`, com `OPERATIONAL_AUTHORIZATION=BLOCKED` e `ENVIRONMENT_ATTESTATION_COMPLETE=false`. A revisao tecnica foi feita pelo mesmo executor, nao e independente e nao prova o schema final de DEV ou PROD.

  **delta-061, derivacao canonica offline verificada.** A derivacao canonica foi reproduzida e verificada somente offline, repetida A/B em PostgreSQL 17 descartavel sobre a base `07d2c05c687d1a0e8deeacbb7f8b16fbdd0e4e86`; 75/75 migrations produziram os mesmos 388390 bytes, SHA-256 `7040a54d80c0ee4f37e1986ff0a579db275e45c129f4fdafcd66788e22a3eb3e` e fingerprint `8ac17d4352a77fb3c5885f9c1a55813a5b7dfcd6fb84c4bd4e9117c1c7883370`. A decisao e as limitacoes estao em `docs/decisions/2026-08-29-offline-canonical-schema-derivation.md`. Isso nao atesta DEV, PROD, Data API ou Realtime; `OPERATIONAL_AUTHORIZATION=BLOCKED` permanece obrigatorio.

  **delta-062, derivador e fingerprint integrados.** A PR #334, HEAD `a864730f0b678cca39cebfa6bb378243ba031cd6`, foi integrada no merge `c8427b1a505c0aad2a5f675d3bf456ee33716690`; o Git registra `commit date=2026-08-29T21:21:15Z`, e o GitHub registra `mergedAt=2026-08-29T21:21:16Z`. Os seis checks da PR e os seis pós-merge concluíram com `SUCCESS`; os detalhes da API do deployment automático Vercel frontend Production `6160229001` estão em `docs/decisions/2026-08-29-offline-canonical-schema-derivation.md`. Os checks provam apenas o comportamento exercitado naquele SHA; a metadata do deployment prova somente o frontend e não prova backend, banco, migration, runtime ou atestação de ambiente.

  **delta-063, tooling de atestacao read-only integrado e comprovado offline, ambientes nao consultados e operacao bloqueada.** A ferramenta separada foi implementada no commit tecnico `be958ce96e65d3d497923b7f5f912676634e9587`, sobre a base `1072e6a8e85d201a1c82f37a8ddeac5417300c49`. A prova focal offline passou em `81/81`, a selecao relacionada terminou em `367 passed, 47 skipped` e a prova focal em PostgreSQL 17 TLS descartavel passou em `82/82`. Sarah/Terra concluiu `GO`; o healthcheck do Claude Opus passou, mas a revisao completa travou com `Execution error` e nao foi reclassificada como revisao concluida. A PR #337, HEAD `abf6f823336b81e93ec1c942dcd5a357d8ac797c`, integrou o tooling no merge `278afb205a3b4735d4aeb66e2e585f71fd562ef7`, com `mergedAt=2026-08-30T11:38:16Z`. Os sete workflows do push em `main` concluiram com `SUCCESS`: Environment Attestation PG17 `33309430738`, Frontend CI `33309430763`, Canonical Schema Derivation `33309430775`, Backend Tests `33309430797`, Tooling Static Checks `33309430744`, E2E Critical `33309430731` e RLS Integration `33309430799`. A Vercel registrou o deployment frontend Production `6166209567`, com `state=success`; o deployment e seu status registraram `created_at=2026-08-30T11:39:02Z`. Essa metadata prova somente o frontend e nao prova backend, banco ou runtime. O estado corrente e `INTEGRADO E COMPROVADO OFFLINE / AMBIENTES NÃO CONSULTADOS / OPERAÇÃO BLOQUEADA`. Nenhum DEV ou PROD foi consultado e nenhum artefato ambiental foi produzido. O schema JSON valida somente o envelope e o verificador Python e obrigatorio. O HMAC serve para correlacao e anti-swap, nao substitui autorizacao humana nem observa diretamente o project ref. Data API e Realtime permanecem `PLATFORM_SURFACES_UNATTESTED`. `OPERATIONAL_AUTHORIZATION=BLOCKED` e `environment_attestation_complete=false` permanecem obrigatorios. A decisao esta em `docs/decisions/2026-08-30-read-only-environment-attestation-tooling.md`.

  **delta-064, runner DEV preflight-only integrado e comprovado offline, ambientes nao consultados e operacao bloqueada.** Sobre a base `fe7dcd394bd1cfdc96204ad994bcba9f0c96adb4`, os SHA-256 congelados sao: runner `1973aab6c6af09105acfbfe03396b048c389d059ae87ff1b673198ba35fb280f`, testes unitarios `d96fab1afe99531e3cee0f84bc285876de303ed0265fa41c51f8da9a7bcab0a0`, prova PG17 `ceecfe9afa09066e4863e93be556b8f92c00a2992e0a0aef3b4253458f6fc318`, testes de atestacao existentes `68f9790a734f8adf78db8a716a5c2d99adad165f00737f922db90afa614b4ed8` e workflow `80c53134e91a4221201052ff6c6782f76cdcaa9968c3406a46c3bca16e878ddf`. Os unitarios passaram em `210/210`; duas provas locais sequenciais no PostgreSQL 17 TLS passaram em `1/1` para a atestacao existente e `1/1` para o runner com CA por FD. A PR #340, HEAD `b29d3f494eabc3a04fe7f2c434758ad274f03930`, integrou o runner no merge `82413edb884125d4d8f6e7946ffcaaf48ed8491c`, com `mergedAt=2026-08-30T13:55:11Z`. Os sete workflows pos-merge concluiram com `SUCCESS`: E2E `33315460948`, Frontend `33315460933`, Tooling `33315460941`, RLS `33315460942`, Backend `33315460949`, Environment Attestation PG17 `33315460934` e Canonical Schema Derivation `33315460939`. A Vercel registrou o deployment frontend Production `6167369343`, com `state=success`, em `2026-08-30T13:55:56Z`. Essa metadata prova somente o frontend e nao prova backend, banco ou runtime. O contrato usa `TLS_MODE=VERIFY_FULL_EXPLICIT_CA` e vincula o digest da CA `TLS_CA_CERTIFICATE_SHA256` a autorizacao. `PROCESS_INVOCATION_ONLY` exige nova autorizacao nominal para cada invocacao. O HMAC serve somente correlacao e anti-swap e nao substitui autorizacao humana. O resultado produz zero arquivo, zero recibo, zero captura e zero materializacao. Os buffers de chave e nonce sao zerados, os descritores sao fechados e os certificados TLS temporarios sao removidos apos a prova. DEV e PROD nao foram consultados. PROD esta explicitamente fora. PROD continua fora. Estado: `INTEGRADO E COMPROVADO OFFLINE / DEV/PROD NÃO CONSULTADOS / OPERAÇÃO BLOQUEADA`.

  **delta-065, duas invocacoes DEV bloqueadas e diagnostico offline sem causa inferida.** Em 2026-08-30, ja no `main` `64cc157d649256a4a9819741f4276c0420590fd1`, duas invocacoes DEV foram feitas sob autorizacoes humanas nominais distintas e exclusivas, cada uma limitada a `PROCESS_INVOCATION_ONLY`. O timestamp operacional preciso nao foi preservado e nenhum horario UTC foi inferido. Ambas terminaram com exit `7`, `RESULT=BLOCKED_DATABASE_PREFLIGHT_FAILED`, `ROLLBACK_CONFIRMED=false` e `CONNECTION_CLOSED=true`. Em ambas, `OPERATIONAL_AUTHORIZATION=false`, `NEXT_STAGE_AUTHORIZED=false`, `CAPTURE_EXECUTED=false`, `MATERIALIZATION_EXECUTED=false` e `PROD_ACCESSED=false`. Esses campos nao provam se houve conexao, nao provam sucesso ou falha de autenticacao e nao identificam a causa raiz. O diagnostico posterior passou em `2/2` no caminho full-main sobre PostgreSQL 17 TLS descartavel e em `97/97` no foco offline. O runner permaneceu intacto, SHA-256 `1973aab6c6af09105acfbfe03396b048c389d059ae87ff1b673198ba35fb280f`, assim como o workflow, SHA-256 `80c53134e91a4221201052ff6c6782f76cdcaa9968c3406a46c3bca16e878ddf`. A prova PG17 ampliada tem SHA-256 `ddbc092216604e65cf86070d409837c7d328da96116ae5ea8d0947195b421b9e`. Essa prova local nao reclassifica DEV nem determina a causa do bloqueio. A evidencia detalhada esta em `docs/decisions/2026-08-30-dev-identity-preflight-diagnostics.md`. Estado: `DUAS INVOCACOES DEV BLOQUEADAS / CAUSA NAO DETERMINADA / PROD NAO CONSULTADO / OPERACAO BLOQUEADA`.

  **delta-066, diagnostico DEV integrado e bloqueio preservado.** A PR #342, HEAD `5076c47b19fffe503e823d68c6dadfc59b11ed5d`, integrou a prova diagnostica no merge `bc202da6c0ef83e03ded4392e508441cd4d6a188`, com `mergedAt=2026-08-30T15:24:45Z`. Os sete workflows pos-merge concluiram com `SUCCESS`: Canonical `33319560819`, Environment Attestation PG17 `33319560923`, E2E `33319560908`, RLS `33319560769`, Backend `33319560836`, Frontend `33319560781` e Tooling `33319560786`. A Vercel registrou o deployment frontend Production `6168185324`, com status `17531418022`, `state=success` e `created_at=updated_at=2026-08-30T15:25:32Z`. Essa metadata prova somente o frontend e nao prova backend, banco ou runtime. A integracao nao repetiu o preflight, nao consultou logs, nao fez novo acesso a DEV ou PROD e nao determinou a causa do exit `7`. Runner e workflow permanecem intactos. Estado: `INTEGRADO E COMPROVADO OFFLINE / DUAS INVOCACOES DEV BLOQUEADAS / CAUSA NAO DETERMINADA / PROD NAO CONSULTADO / OPERACAO BLOQUEADA`.

  **delta-067, fase sanitizada do preflight integrada e comprovada offline.** Sobre a base `3685bbcaf11d5a20b3492953d897cb6a459701a8`, o candidato adicionou o enum estatico `PREFLIGHT_FAILURE_PHASE`; a PR #344 o integrou no `main` `bab031a7e0067a257eedb4a24c786cc925801463`. A fase e somente a ultima fronteira operacional iniciada, nunca a causa, e `CONNECT_TLS_AUTH` nao prova nem separa DNS, TCP, TLS, CA, senha ou autenticacao. Os SHA-256 congelados sao runner `8da631fbb602488bb8c82ce1529c9d8ba17acbae8a318ea9b0fc24cdd8f65cd2`, unitarios `c55726f0ad8abf7680de868cba155388f7e56773aa8054e556be89dc87aa90a8` e PG17 `d86037d759d254581d2259026585ac768e4b2d68595473371ec65daf6c6de5a9`. Passaram `109 passed, 2 skipped` offline, `2/2` em PostgreSQL 17 TLS descartavel e `222 passed, 2 skipped` no agregado relevante; Sarah concluiu `GO`. As duas execucoes DEV historicas nao foram retroclassificadas. A decisao esta em `docs/decisions/2026-08-30-dev-preflight-failure-phase-diagnostics.md`.

  **delta-068, terceira invocacao DEV bloqueada e probe de transporte preparado somente offline.** Em `2026-08-31`, uma unica invocacao DEV `PROCESS_INVOCATION_ONLY` no `main` `bab031a7e0067a257eedb4a24c786cc925801463` terminou com exit `7`, `RESULT=BLOCKED_DATABASE_PREFLIGHT_FAILED` e `PREFLIGHT_FAILURE_PHASE=CONNECT_TLS_AUTH`. A autorizacao era valida entre `2026-08-31T11:03:30Z` e `2026-08-31T11:18:30Z`; essa janela nao e o horario da execucao, cujo timestamp preciso nao foi preservado nem inferido. DNS, TCP, TLS, CA, senha, autenticacao, endpoint, disponibilidade, conexao, transacao e identidade permanecem `UNKNOWN`. A autorizacao foi consumida; nenhum log foi consultado e nao houve retry, captura, materializacao, DML, migration, backfill, deploy, flag, runtime ou acesso a PROD. A limpeza removeu o diretorio temporario de autorizacao, o launcher e a worktree operacionais temporarios; o checkout ficou limpo, sem `__pycache__` ou `.pyc`, e o registro Git obsoleto da worktree foi removido. O probe para separar somente DNS, TCP e TLS foi planejado offline, permanece `execution_disabled=true` e nao foi executado. A decisao esta em `docs/decisions/2026-08-31-dev-connect-tls-auth-transport-probe.md`. `OPERATIONAL_AUTHORIZATION=false` e `NEXT_STAGE_AUTHORIZED=false` permanecem obrigatorios.

  **delta-069, plano offline CONNECT_TLS_AUTH integrado.** A PR #346, HEAD `0c63dc29dc903e0e7012b9fb811b7b2ddb05ab51`, foi integrada no merge `fb776e270bf3e2ffde0cbb28e400960591b74420`, com `mergedAt=2026-08-31T13:02:07Z`. Os sete workflows pos-merge concluiram com `SUCCESS`: Tooling `33394774001`, Environment Attestation PG17 `33394774013`, Canonical `33394773986`, E2E `33394774109`, Frontend `33394774063`, RLS `33394773965` e Backend `33394774029`. A Vercel registrou o deployment frontend Production `6181597461`, status `17569033825`, `state=success`, em `2026-08-31T13:02:53Z`. Essa metadata prova somente o frontend e nao prova saude funcional, backend, banco, DEV, PROD, probe ou migration. A integracao versionou apenas o plano offline: `execution_disabled=true`, implementacao e capacidade de rede ausentes, probe nao executado e operacao bloqueada.

  **delta-070, reconciliacao pos-merge e candidato de probe transport-only.** A PR #347, HEAD `0a257e9aa1985860d5ea0a4506d4f7e84c7b2312`, foi integrada no merge `36f8d13284a8f4964d0258a2a3b845323a80fe7e`, com `mergedAt=2026-08-31T14:26:10Z`; os sete workflows pos-merge concluiram com `SUCCESS`. O deployment automatico Vercel frontend Production `6183047421`, status `17572803614`, terminou com `state=success` em `2026-08-31T14:26:57Z`; essa metadata prova somente o frontend. Sobre o merge, o candidato implementa `backend/scripts/probe_dev_connect_tls_auth_transport.py`, SHA-256 `4196e218e023f5ef16fe333f62b756b55239d0bdde1c11aed12e59af888f6cc9`, e sua matriz adversarial, SHA-256 `b79ff9d7473fdafd0a4fcd6ceba98b2c46f5470ef517b6663898812fe8b1296e`. Passaram `90/90` testes exclusivamente offline, incluindo loopback TLS sintetico descartavel. O runner recebe seis descritores privados, fixa o hash do project-ref DEV e do registro de autorizacao, envia somente o SSLRequest PostgreSQL de oito bytes, exige `S`, valida CA e hostname e fecha antes de StartupMessage. Nao recebe senha, usuario, banco ou DSN e nao tenta autenticacao nem SQL. O plano JSON permanece historico e byte-identico; `execution_disabled=true` e `implementation_present=false` descrevem a etapa anterior ja consumida. A unica rede desta rodada foi o `git fetch` nominal autorizado para obter o merge; nenhum probe vivo, DEV, PROD, banco ou log foi acessado. `operational_authorization=false` e `next_stage_authorized=false` permanecem.

  **delta-071, implementacao do probe transport-only integrada.** A PR #348, HEAD `af91e5218f9317a730aa29ad8d8c645312b30f19`, foi integrada no merge `1e727cd2ea90ccfb68961174b802d595c71f355b`, com `mergedAt=2026-08-31T15:22:49Z`. Os sete workflows pos-merge concluiram com `SUCCESS`: Tooling `33408103314`, Environment Attestation PG17 `33408103217`, Canonical `33408103386`, Frontend `33408103193`, E2E `33408103279`, Backend `33408103254` e RLS `33408103282`. A Vercel registrou o deployment automatico frontend Production `6184050276`, status `17575418445`, `state=success`, em `2026-08-31T15:23:35Z`. Essa metadata prova somente o deployment do frontend, nao sua saude funcional, e nao prova backend, banco, DEV, PROD ou o probe. O estado agora e `IMPLEMENTADO / INTEGRADO / COMPROVADO OFFLINE / PROBE NAO EXECUTADO / OPERACAO BLOQUEADA`.

  **Gate consumido em 2026-08-31:** `SEPARATE_NOMINAL_DEV_CONNECT_TLS_AUTH_TRANSPORT_PROBE_AUTHORIZATION`. Seu consumo exigiu nova autorizacao humana nominal para exatamente uma invocacao no checkout de `main` `1e727cd2`, com `source_main_git_sha=36f8d13284a8f4964d0258a2a3b845323a80fe7e`. O gate nao autoriza retry, senha, autenticacao, sessao de banco, SQL, logs, captura, materializacao, DML, migration, reconciliacao, backfill, deploy manual ou Production, flag, runtime ou PROD. Uma unica invocacao terminou com exit `7`, fase `TLS_HANDSHAKE` e `RESULT=BLOCKED_DEV_CONNECT_TLS_AUTH_TRANSPORT_PROBE:TRANSPORT_BLOCKED`. DNS, politica de endereco, TCP e a resposta `S` ao SSLRequest foram confirmados; handshake e hostname nao foram confirmados. Nao houve retry, senha, autenticacao, sessao de banco, SQL, logs ou PROD. A causa permanece indeterminada e o resultado nao recebe categoria retroativa. A evolucao offline adiciona somente uma categoria estatica de falha TLS, com runner SHA-256 `0ac585b86dd1c96446622e9a46bccda8a1e43eb0bceb0dcc19226892cb88d191`, testes SHA-256 `70334dfc33505ea0b5ddb85a6406672fe0d9154e105134da164c773978459489` e `95/95` testes verdes.

  **delta-072, categoria TLS integrada e desenho migration-epoch v3 ainda somente offline.** A PR #350, HEAD `58af39b760b8b5be85723d3ea693abd20fe3f3cf`, foi integrada no merge `0f8c6a77bf489f9080743ab3f7ce71097d361aea`, com `mergedAt=2026-08-31T16:38:27Z`. Os sete workflows pos-merge concluiram com `SUCCESS`: Backend `33415223927`, Canonical `33415223885`, E2E `33415223922`, Environment Attestation PG17 `33415223904`, Frontend `33415223881`, RLS `33415223955` e Tooling `33415223892`. A Vercel registrou o deployment automatico frontend Production `6185328714`, status `17578739446`, com `SUCCESS`; essa metadata prova somente o deployment do frontend, sem provar saude funcional, backend, banco, DEV, PROD, probe, migration ou runtime. O gate `REVIEW_AND_CI_DEV_TLS_HANDSHAKE_FAILURE_CATEGORY_PR` foi consumido pela PR #350. A categoria TLS esta integrada e comprovada offline; o resultado historico nao recebe categoria retroativa e a causa permanece indeterminada. A arvore do merge e identica a do HEAD da PR. O desenho `migration-epoch v3` devera tratar como `KNOWN_UNVERIFIED_DRIFT`, sem nova consulta nem inferencia de migration aplicada, os sete indices observados por evidencia operacional anterior: `idx_pessoas_igreja_ativa_created`, `idx_pessoas_igreja_ativa_tipo`, `idx_celulas_igreja_ativo_lider`, `idx_work_queue_igreja_status_responsavel`, `idx_conversations_igreja_assumido`, `idx_app_users_igreja_nome` e `idx_user_roles_igreja_user`. Essa observacao nao foi revalidada nesta missao e nao prova o estado atual de DEV. A atestacao v1 valida somente envelopes que continuam bloqueados; ela nao comprova conclusao e nao pode ser reinterpretada como `environment_attestation_complete=true`. Os artefatos historicos v1 e v2 permanecem byte-identicos e fora do escopo. O pacote candidato `migration-epoch v3` esta congelado como `OFFLINE_EPOCH_CUTOVER_DECISION_PACKAGE_BLOCKED`: verificador `backend/scripts/verify_migration_history_divergence_remediation_proposal_v3.py`, SHA-256 `8d7712be4f63ead2eff2c9e7af236e610b0c148acb07c85ebcd81db1f6d0877d`; teste `backend/tests/test_migration_history_divergence_remediation_v3.py`, SHA-256 `b34bd0677feb9d4453477d7503dc19beffcaf6cc8648acb85be56113b7578e24`; proposta `docs/governance/migrations/migration-history-divergence-remediation-proposal-v3.json`, SHA-256 `076d04ed179c5128c4707c07cacd8240896101a9bea62e328d2d0569900cd10e`; e schema `docs/governance/migrations/migration-history-divergence-remediation-proposal-v3.schema.json`, SHA-256 `88f7972780f07c7071bb4e4292e1f21c258fff47daf2ab207fc709ff34631b38`. A matriz nova passou `87/87`, a focal estavel passou `138/138`, e o verificador terminou fail-closed com exit `8` e `RESULT=BLOCKED_MIGRATION_EPOCH_V3:PENDING_SEPARATE_EVIDENCE`. O estado e `RECOMMENDATION_ONLY_NOT_APPROVED`; isso comprova somente o desenho offline e nao autoriza evidencia viva, cutover, migration ou runtime.

  **delta-073, correcao de timeout e contrato D3 integrados pela PR #351.** A correcao de precedencia classifica `TimeoutError` e `socket.timeout` como `DEADLINE_EXCEEDED` antes de `OSError` generico em cada fronteira de rede. O batch integrado tem runner SHA-256 `2e2208bfbca1214c0cec024c58716eeac7c05789c33ce36d812c0265c3810809`, teste SHA-256 `d7161cd7dd7c63935c07431193b0d916222e5341088edbdc6d4ef85ad3063689` e `102/102` testes verdes; nenhum probe vivo foi executado. Os hashes da PR #350 `0ac585b86dd1c96446622e9a46bccda8a1e43eb0bceb0dcc19226892cb88d191` e `70334dfc33505ea0b5ddb85a6406672fe0d9154e105134da164c773978459489` permanecem evidencia historica e nao sao substituidos. O contrato D3 fail-closed integrado usa `backend/app/agent/private_checkpoint.py`, SHA-256 `098d7186d59b2be9c231e3ca41e328b69901d4bc3e3f9b09651b902c07768f33`, `backend/app/agent/context.py`, SHA-256 `b8d9ccea0041a81021cb2b4cf8edcbd8af0457ebf4401b021bd974edd29eea7d`, e `backend/tests/test_agent_private_checkpoint_contract.py`, SHA-256 `2f91523e6a5daacd7c3ac08b933c7d9f857c3eec2a72b9f962c09c98d39f3c8b`. A selecao `tests/test_agent*.py` terminou em `292 passed, 7 skipped`, com duas advertencias preexistentes. A classificacao e `CONTRATO OFFLINE INTEGRADO E INATIVO`: nao ha saver, migration ou wiring, e o LangGraph continua stateless.

  A PR #351 foi integrada no merge `bc97dd4e6f2fc9024e85afe8d611708699c8983a`. Os `7/7` checks pos-merge concluiram com `SUCCESS`. A Vercel registrou o deployment automatico do frontend Production `6187006353`, status `17583083885`, com `SUCCESS`. Essa metadata prova somente o frontend e nao prova backend, banco ou runtime. A preparacao D3 de estado efemero desta branch permanece candidata offline, sem saver, migration ou retomada, e nao integra a evidencia pos-merge da PR #351.

  A PR #352, HEAD `c5b2b4c775592641b308de6b2ac3cd069f34dcb3`, integrou essa preparacao no merge `6c807717010a41edf3bfd3d1b2405c2f3527a696`, cuja arvore e identica a do HEAD da PR. Os `7/7` workflows pos-merge concluiram com `SUCCESS`: Backend Tests `33428905043`, Canonical Schema Derivation `33428905057`, E2E Critical `33428905042`, Environment Attestation PG17 `33428905234`, Frontend CI `33428905212`, RLS Integration `33428905114` e Tooling Static Checks `33428905041`. A Vercel registrou o deployment automatico do frontend Production `6187746800`, status `17584957483`, com `SUCCESS`, em `2026-08-31T19:09:09Z`. Essa metadata prova somente o frontend e nao prova saude funcional, backend, banco, saver, migration, memoria ativa, deploy do backend, flag ou runtime. O estado permanece `PREPARACAO D3 INTEGRADA E INATIVA`.

  Sobre esse merge, o commit tecnico local `14b3d7ba15e88032cd53714008d36badd4578e80` introduziu o contrato puro `AgentTurnIdentity` e `AgentEffectIntent`.

  Na branch local, o commit `f82f76927ba8a6a265478ad7f21eae07b0d6504c` adiciona somente o adapter confiavel de entrada, protegido por `agent_trusted_inbound_identity_enabled=false` por padrao. O `Message.id` da entrada persistida agora chega em `IngestionOutcome` nos caminhos novo e duplicado. Antes de sessao, reserva, lease, runtime ou qualquer outro I/O, o worker constroi a identidade com igreja, conversa, mensagem e ID Evolution persistidos. Antes da primeira consulta, o runtime rederiva a identidade com quatro entradas confiaveis e separadas: `igreja_id`, `conversation_id`, o UUID inbound persistido de `Message.id` e o `provider_message_id` exato. Ele exige igualdade integral dos quatro vinculos com a identidade construida pelo worker; qualquer divergencia aborta. `claim_id` permanece requisito separado de recuperacao, nunca entra no `turn_id`; o caminho legacy e preservado somente com a flag desligada. O estado do grafo continua recusando aliases de autoridade e nao recebe essa identidade.

  O mesmo lote incorpora em `7d1ed00d0add18162a89f3a9c39da6039e74017c` o contrato puro e inativo `turn_execution`, originalmente revisado em `576de558983622146a91417c65a85a2a321f585b`. Ele define plano canonico, ordem versionada de efeitos, escopo opaco por tenant e conversa, recibos estruturais, maquina pura da futura outbox de resposta e chave atual `v2`. Nada disso persiste plano ou recibo, autentica uma store, serializa turnos, garante FIFO ou cria atomicidade entre commit de dominio e outbox. `ACCEPTED` significa somente aceite do transporte; `AMBIGUOUS` e terminal. Evidencia legacy `v1` ou `v0` nao e derivada nem autenticada pelo contrato.

  Os pins atuais sao `backend/app/agent/turn_identity.py`, SHA-256 `5be323d7fafa4a51d5c954749c8d2d5991e33313e269ee0a3b63bdfc9fb3923d`; `backend/tests/test_agent_turn_identity.py`, SHA-256 `4072b76688552b6f870e89876426d3c608b34a362ec895315d733691dff101c5`; `backend/app/agent/turn_execution.py`, SHA-256 `72a53515a835bac528280223e22f76a33f8606b5ce979dae11773d10ea6a1b2b`; e `backend/tests/test_agent_turn_execution.py`, SHA-256 `7e22814f1715b7bdfc7f83431bf4e15cdf6d8f7d13d0d8d3afaa6811e95e0b2d`. O wiring passou em `245/245` e em `401 passed, 7 skipped` na selecao `tests/test_agent*.py`; o contrato de execucao passou em `86/86`, na revisao independente `190/190` e em `462 passed, 7 skipped` na mesma selecao. As duas revisoes terminaram `GO`, sem P0, P1 ou P2. A evidencia e local e pre-PR.

  Na mesma branch, o commit tecnico local `abafdffdc8252fa6dff7c9d1975cb6c241141971` adiciona o adaptador puro e replay-only `turn_plan_adapter`. Ele projeta a saida fechada do grafo em um plano deterministico, mas nao oferece status `EXECUTABLE`, callback injetavel ou consumer de runtime. Plano armazenado ausente ou qualquer receipt terminal ausente produz `FIRST_EXECUTION_UNSUPPORTED` e bloqueia a primeira execucao. Somente um plano armazenado estruturalmente exato e vinculado ao digest, junto de um receipt terminal valido para cada efeito, retorna `REPLAY_TERMINAL`; esse resultado nao concede execucao, persistencia, transporte, retry ou mutacao de dominio. `tool_calls` permanecem bloqueados. A oferta do relatorio e aceita somente quando finita, nao negativa e exata em centavos, sendo vinculada como inteiro `oferta_centavos`.

  Os novos pins sao `backend/app/agent/turn_plan_adapter.py`, SHA-256 `c81dafec100734ee9a219d8c99a636636b6317b94c93c87cb89ba0f9af581002`; e `backend/tests/test_agent_turn_plan_adapter.py`, SHA-256 `328f3a2870fab8ea38f1901a02e640bec2f5bc9457c3d5261f350a45ef560d5e`. A revisao integrada passou em `291/291`; a selecao `tests/test_agent*.py` terminou em `625 passed, 7 skipped`. A revisao concluiu `GO`, com P0, P1 e P2 iguais a zero. Essa evidencia e local e pre-PR.

  O lote ampliado permanece exclusivamente offline. O wiring de identidade continua inativo porque a flag fica desligada por padrao e nenhuma ativacao ocorreu. `turn_execution` e `turn_plan_adapter` nao possuem consumer de runtime e executam zero I/O. Nao existem saver, checkpoint duravel, migration, plano ou receipt persistido, FIFO, bloqueio serial real, atomicidade entre efeitos, retomada, primeira execucao ou memoria ativa. Estado: `LOTE D3 OFFLINE AMPLIADO LOCALMENTE / REPLAY-ONLY / FLAG DEFAULT FALSE / CANDIDATO NAO INTEGRADO NO MAIN / RUNTIME NAO ATIVADO`.

  O gate historico `REVIEW_AND_CI_OFFLINE_AGENT_FOUNDATION_BATCH_PR` foi consumido pelo push, abertura, CI e Preview da PR #351. Ele nao autorizou o merge posterior, permanece somente como evidencia historica e nao e um segundo gate corrente.

  O gate historico `REVIEW_AND_CI_D3_EPHEMERAL_EFFECT_STATE_PR` foi consumido pelo push, abertura, CI e Preview da PR #352. O merge e o deployment automatico do frontend Production foram autorizados separadamente; esse gate nao os autorizou. Apos o consumo, ele permanece somente como evidencia historica e nao e um segundo gate corrente.

  O gate anterior `REVIEW_AND_CI_D3_TURN_IDENTITY_OFFLINE_PR` foi substituido localmente, sem consumo, pelo lote combinado. Nao houve push, PR, CI ou Preview sob esse gate, portanto ele nao e evidencia historica de uma acao externa.

  O gate anterior `REVIEW_AND_CI_D3_TURN_EXECUTION_AND_TRUSTED_INBOUND_WIRING_OFFLINE_PR` foi substituido localmente, sem consumo, pelo lote ampliado replay-only. Nao houve push, PR, CI ou Preview sob esse gate, portanto ele nao e evidencia historica de uma acao externa.

  **Próximo gate único:** `REVIEW_AND_CI_D3_TURN_FOUNDATION_REPLAY_ONLY_OFFLINE_PR`. O nome nao constitui autorizacao ja concedida. Seu consumo exige autorizacao humana posterior e separada que nomeie push, abertura da PR e GitHub CI e aceite o Vercel Preview automatico. O gate cobre somente revisao e CI do lote offline ampliado replay-only. Nao autoriza merge, Vercel Production, flag-on, runtime, saver, probe vivo, acesso a DEV ou PROD, banco, logs, SQL, DML, migration, deploy, mensagem, tool call ou qualquer efeito vivo.

  A PR #320, HEAD `66ce06d9a356a52e63366b3a6528b0b83170d12e`, foi integrada no merge `947d891c2ea278b7a3231fecd9ca1c90cfe29a1f`. Os cinco workflows da PR e os cinco pos-merge ficaram verdes. O merge gerou o deployment automatico Vercel frontend Production `6140373952`, com `SUCCESS`; essa metadata prova somente o frontend nesse ambiente e nao prova backend, banco ou Supabase. Esta missao nao aplicou a migration D2B2b3A; DEV e PROD confirmaram a ausencia. A flag permanece `false`, e nao houve deploy manual ou do backend, wiring, ativacao ou canario.

  **Estado de implementacao na data da decisao:** `backend/app/agent/graph.py` compila o grafo sem checkpointer duravel; `AGENT_GRAPH_CHECKPOINT_URL` somente gera warning. `backend/app/agent/nodes.py` captura o resumo do relatorio como evento, mas nao grava o relatorio canonico de `celula_reuniao`. RAG institucional, consentimento por finalidade, memoria derivada com exclusao integral, especialistas de dominio e a vertical transacional do relatorio continuam pendentes.

  **Ordem de entrega aprovada:** `D2B1`, contexto confiavel v1; `D2B2a`, ledger inativo de consentimentos por finalidade; `D2B2b1`, fronteira pura e deny-first, sem migration ou caller, com chave idempotente opaca gerada no servidor e toda concessao negada; `D2B2b3A`, persistencia, API e painel do Console Master limitados a rascunhos por igreja; depois, em fluxo nominal separado, pacote humano e juridico aprovado por finalidade; somente em fatia posterior, catalogo imutavel, prova correlacionada, retencao, RBAC e writers server-side seguros; `D2C`, propostas duraveis e confirmacao; `D3`, memoria privada e exclusao integral; `D4`, conhecimento oficial e onboarding; `D5`, outbox e notificacoes; depois o relatorio de celula pelo WhatsApp e, em seguida, Central de Celulas, Agenda e Consolidacao. Universidade da Vida e Capacitacao Destino continuam no horizonte integral, fora da missao atual e condicionadas a PRDs e missoes proprias. Cada fatia exige testes de isolamento entre pelo menos dois tenants e nao abre gates de envio, broadcast, Brevo ou Asaas por implicacao.

  **Estado integrado D2B2a, D2B2b1 e D2B2b3A em 2026-08-28:** a PR #317, HEAD `8ba5c988e9169703c923b1f1a3e47d1c427531e1`, foi integrada no merge `bce5a9a434077e488cea8baae3e9dd7c7c4ba0f1`. Migration, ORM, dominio e servico interno sem caller materializam o ledger append-only `public.consentimento_finalidade_evento`. O contrato usa as quatro finalidades canonicas, estados `concedido|retirado`, `versao_termo`, fontes v1 `whatsapp_inbound|painel_autenticado`, operador obrigatorio somente para o painel no INSERT inicial, idempotencia por tenant e sequencia por stream protegida no banco. A exclusao referencial posterior do AppUser pode anonimizar o operador via `ON DELETE SET NULL`, preservando o evento. A RLS e forcada com barreira restritiva GUC-only e ACL minima. Nao existe backfill, API, wiring ou caller, e a migration nao foi aplicada em Supabase DEV ou PROD. A PR #318, HEAD `ede4797003e044f582da9f9a3ab86554f708a73a`, foi integrada no merge `74951828f48994622a112d8e59eb978e5fb4f406` e adiciona somente fronteira pura, sem migration ou caller: chave opaca server-side, RBAC deny-first e toda tentativa de `concedido` recusada. A PR #320, HEAD `66ce06d9a356a52e63366b3a6528b0b83170d12e`, foi integrada no merge `947d891c2ea278b7a3231fecd9ca1c90cfe29a1f` e adiciona apenas o workspace draft-only do Console Master, ainda inativo. Esta missao nao aplicou a migration D2B2b3A; DEV e PROD confirmaram a ausencia. Os cinco workflows de cada PR e os cinco pos-merge ficaram verdes. As PRs geraram Preview e os merges geraram deployment frontend Vercel automatico classificado como Production. Essa metadata prova o deployment do frontend no ambiente Production da Vercel; nao prova backend, banco ou Supabase. Textos e versoes, hipotese juridica, prova, retencao, eliminacao e regras de opt-out continuam decisoes humanas e juridicas obrigatorias antes de catalogo ou writer. O template D2B2b2 permanece `TEMPLATE_ONLY / NOT_APPROVED`; a D2B2b3A nao satisfaz o gate humano. As decisoes detalhadas estao em `docs/decisions/2026-08-28-d2b2-purpose-consent-ledger.md`, `docs/decisions/2026-08-28-d2b2b1-consent-security-boundary.md`, `docs/decisions/2026-08-28-d2b2b2-consent-decision-packet-contract.md` e `docs/decisions/2026-08-28-d2b2b3-master-governance-drafts.md`.

---

## 8. Dependencias e Riscos

### Dependencias externas
- **Clerk** — autenticacao e sessao (US-01). Sem app nativo; integracao por estados de UI no design.
- **Supabase (Postgres + RLS)** — persistencia e isolamento multi-tenant (US-02, RNF-02/RNF-21).
- **Evolution API** — conexao e troca de mensagens com o WhatsApp oficial (US-05..US-08); processo sempre-ligado.
- **Provedor LLM (OpenAI, BYO)** — respostas do agente; custo a cargo da igreja (US-27, RNF-20).
- **LangGraph / FastAPI + worker de filas** — orquestracao do agente e processamento de webhooks (RNF-08/RNF-15/RNF-17).
- **Brevo** — e-mails de convite/ativacao (US-03).
- **Google Calendar** — sincronizacao de eventos (US-30).
- **Asaas** — checkout e cobranca (PIX/boleto/cartao), setup fee e status de assinatura (US-34..US-36).
- **Infra de containers (Coolify/Dokploy) em VPS unica >= 4GB** — TLS automatico e restart de processos (RNF-04/RNF-15/RNF-18).

### Riscos e mitigacoes
- **Queda da conexao WhatsApp** -> perda de mensagens. *Mitigacao:* monitoramento de status, reconexao (US-06) e worker de filas com reprocessamento, sem duplicar contatos (RNF-16/RNF-17).
- **Latencia/custo/indisponibilidade do LLM** afetando o atendimento. *Mitigacao:* timeout de roteamento separado do tempo do LLM (RNF-08), credencial BYO validada antes de ativar (US-27) e logs de consumo por igreja (RNF-24).
- **Vazamento entre tenants** (dado de uma igreja em outra). *Mitigacao:* RLS desde a 1a linha + revalidacao de `igreja_id` e papel no backend (RNF-02/RNF-21/RF-D7).
- **Complexidade do RBAC por papeis acumulados + matriz de Permissoes** podendo gerar acesso indevido. *Mitigacao:* autorizacao no backend como fonte de verdade; UI e conveniencia (F4/delta-033); admin tem acesso implicito e dashboard garantido a todos.
- **Trilha ministerial automatica** avancando etapas de forma incorreta. *Mitigacao:* maquina de estados unica e auditavel (RNF-22), edicao manual apenas como excecao.
- **Recursos limitados da VPS unica** sob carga. *Mitigacao:* paginacao (RNF-09), processos persistentes com restart, monitoramento de performance.
- **Sensibilidade de dados religiosos/CPF (LGPD).** *Mitigacao:* consentimento/opt-out registrados, versao do termo, re-aceite e mascara de dados sensiveis nos logs (RNF-06).
- **Privacidade pastoral.** *Mitigacao:* captura restrita ao numero oficial; conversas pessoais do pastor nunca registradas (US-07/RF-09).
- **Dependencia de webhooks e crons** para SLAs proativos. *Mitigacao:* infra de agendamento com gatilhos por estado, generica desde o MVP (RNF-23).

---

## Design Lock

> Fonte oficial: `design-contract.json` (hash travado) + `design-brief.md`. Status do lock: **APROVADO** (13/13 regras de validacao). Nenhuma tela, menu ou fluxo fora deste lock deve ser implementado; nenhum requisito aprovado foi removido.

### Direcao visual
- **Direcao:** software web claro, modern-minimal + tech utilitario; base zinc/off-white quente, acento teal dessaturado unico, numeros tabulares, bordas hairline, sem serif e sem preto puro.
- **Densidade:** balanced.
- **Tokens de cor (oklch):** `bg 98.6% 0.003 95` · `surface 100% 0 0` · `sidebar 21% 0.012 200` · `fg 24% 0.01 90` · `muted 52% 0.012 90` · `border 91% 0.005 95` · `accent 52% 0.078 195` · `ok 56% 0.09 155` · `warn 64% 0.11 75` · `danger 56% 0.13 25`.
- **Tipografia:** display/body = system-ui (-apple-system / Segoe UI); mono = ui-monospace / JetBrains Mono.
- **Espacamento:** xs 4 · sm 8 · md 12 · lg 16 · xl 24 · 2xl 32 (px).
- **Radii:** sm 6 · md 10 · lg 14 · xl 20 (px).

### Mapa de telas
Telas oficiais (id — rota — proposito resumido — stories):

**Gestao**
- **login** (`#login`) — autenticar via Clerk. *(US-01)* — estados: idle, loading, error, success.
- **dashboard** (`#dashboard`) — fila de trabalho pastoral, acoes diretas, proximas acoes por responsavel e pendencias com prazo (24h, fonovisita). *(US-02, US-15, US-16, US-17, US-26, US-40)* — estados: loading, empty, populated.
- **inbox** (`#inbox`) — conversas do WhatsApp, fila de atendimento humano e alternancia IA/humano. *(US-08, US-11, US-12, US-13, US-14)* — estados: loading, empty, list, thread-ia-active, thread-human, thread-waiting.
- **calendario** (`#calendario`) — eventos integrados ao Google Calendar. *(US-30)* — estados: loading, empty, month. *(revisado — delta-049: abas semana/mes/ano/a-confirmar/planejamento; import Google = delta-050 / EVT-6.)*
- **comunicados** (`#comunicados`) — envio segmentado respeitando consentimento/opt-out. *(US-31, US-32, US-33)* — estados: compose, segment, review, empty.
- **equipe** (`#equipe`) — convidar/gerenciar pessoas e editar papeis acumulados. *(US-03, US-04)* — estados: loading, empty, list, invite, edit-roles.

**Visao G12 · Igreja (ciclo ministerial)**
- **ganhar** (`#ganhar`) — novos contatos e visitantes (1a etapa). *(US-09, US-10, US-18, US-19, US-20)* — estados: loading, empty, novos-contatos, visitantes.
- **consolidar** (`#consolidar`) — Dashboard de Consolidacao (restrito), fila, lancar decisao por Jesus, lista 100% consolidadas. *(US-18, US-19, US-20, US-37, US-38)* — estados: loading, empty, fila, 100-consolidadas.
- **consol-individual** (`#consol-individual`) — acompanhamento 1:1, avancar etapas/concluir consolidacao. *(US-18, US-19, US-37, US-39)* — estados: loading, empty, fila, detalhe.
- **universidade-vida** (`#universidade-vida`) — turmas/cronograma da UV. *(US-18, US-19)* — estados: loading, empty, turmas, detalhe. **(bloqueada no MVP — ver Restricoes)**
- **capacitacao** (`#capacitacao`) — Capacitacao Destino. *(US-18, US-19)* — estado: locked-em-breve. **(BLOQUEADA no MVP)**
- **g12** (`#g12`) — organograma de descendencias. *(US-21, US-22, US-23)* — estados: loading, empty, organograma, descendencia.
- **central-celula** (`#central-celula`) — lideres, relatorios recebidos/pendentes e comunicacao com lideres. *(US-21..US-26)* — estados: loading, empty, lideres, relatorios.
- **enviar** (`#enviar`) — multiplicacoes (agendadas, sem agendamento), aptos a liderar, aprovacao e historico. *(US-21, US-22, US-23)* — estados: loading, empty, agendadas, sem-agendamento, aptos, historico.

**Configuracao (admin only)**
- **whatsapp** (`#whatsapp`) — conectar via QR, status e reconexao; nao registrar conversas pessoais. *(US-05, US-06, US-07)* — estados: connected, disconnected, reconnecting.
- **agente** (`#agente`) — credencial BYO LLM, comportamento e crons. *(US-27, US-28, US-29)* — estados: behavior, credential, crons.
- **assinatura** (`#assinatura`) — contratar, status e upgrade por porte. *(US-34, US-35, US-36)* — estados: active, past-due, plans.
- **gerentes** (`#gerentes`) — operadores de sistema (papel operacional, distinto do ministerial). *(US-03, US-04)* — estados: loading, empty, list, invite.
- **permissoes** (`#permissoes`) — matriz papel x tela. *(US-04, US-03)* — estados: matrix, saved.

**Superficie separada (stubs de rastreabilidade — fora do painel operacional)**
- **super-admin-igrejas** (`#super-admin-igrejas`) — console multitenant: listar/supervisionar igrejas. *(US-42)* — estados: loading, empty, list, detail.
- **super-admin-provisionar** (`#super-admin-provisionar`) — provisionar nova igreja apos verificar pagamento. *(US-43)* — estados: idle, verifying, provisioned.

> Telas legadas com deep-link valido (fora do menu): `contatos`, `celulas`, `relatorios` — cobertas por `ganhar`/`central-celula`/ciclo G12.

### Navegacao principal
Sidebar (`sidebar-nav`) organizada por grupos:
- **Gestao:** Dashboard (`nav-dashboard` -> dashboard) · Chat (WhatsApp da Igreja) (`nav-inbox` -> inbox) · Agenda da Igreja (`nav-calendario` -> calendario) · Comunicacao (`nav-comunicados` -> comunicados) · Equipe de Lideranca (`nav-equipe` -> equipe).
- **Visao G12:** Ganhar (`nav-ganhar`) · Consolidar (`nav-consolidar`) · Consolidacao Individual (`nav-consol-individual`) · Universidade da Vida (`nav-universidade-vida`) · Capacitacao Destino (`nav-capacitacao`) · G12 · Descendencias (`nav-g12`) · Central de Celula (`nav-central-celula`) · Enviar (`nav-enviar`). Estagios: ganhar, consolidar, discipular, enviar.
- **Configuracao (adminOnly):** Conexao WhatsApp (`nav-whatsapp`) · Agente IA (`nav-agente`) · Assinatura (`nav-assinatura`) · Gerentes de Sistema (`nav-gerentes`) · Permissoes (`nav-permissoes`).
- **Secundaria:** Sair (`nav-logout` -> login). *(US-01)*

Regras de navegacao do lock:
- O menu e o dashboard de cada pessoa sao montados pela **uniao dos papeis acumulados** do seu cadastro; a tela **Permissoes** (matriz papel x tela) e a fonte de verdade dos acessos e reflete em tempo real.
- Itens do grupo **Configuracao** so aparecem para quem tem o papel `admin`.
- Lideres de celula nao acessam o inbox do WhatsApp da igreja.

### Componentes principais
- **btn-primary** (form) — variant primary; estados: default, hover, loading, disabled.
- **form-field** (form) — props label/helper/error; estados: idle, focus, invalid, disabled.
- **sidebar-nav** (navigation) — estados: default, active.
- **status-pill** (display) — tone: ok|warn|danger|accent|muted.
- **work-queue-item** (display) — tipo: visitante|atendimento|relatorio; estados: pending, resolving, resolved.
- **stat-card** (display) — estados: normal, alert.
- **conversation-list** (display) — estados: default, active.
- **conversation-thread** (display) — estados: ia-active, human, waiting.
- **data-table** (display) — estados: empty, populated.
- **tabs** (navigation) — estados: default, active.
- **calendar-month** (display).
- **qr-connect** (display) — estados: connected, disconnected, reconnecting.
- **toggle-switch** (form) — estados: on, off.
- **empty-state** (display).
- **deadline-badge** (display) — tone: ok|warn|late; estados: dentro, alerta, atrasado.
- **decision-modal** (overlay) — props vinculo: celula|visitante; estados: closed, celula-flow, visitante-flow. (em consolidar, consol-individual)
- **assistant-panel** (overlay) — props scope: tenant, roleAware: true; estados: closed, open, thinking. (aside deslizante na topbar, presente nas telas operacionais)

### Estados de UI
- **Carregamento/vazio/populado:** padrao `loading`/`empty`/`populated|list` nas listas (dashboard, inbox, ganhar, consolidar, etc.), com `empty-state`.
- **Inbox/handoff:** `thread-ia-active`, `thread-human`, `thread-waiting`; `conversation-thread` reflete IA ativa, humano e aguardando.
- **WhatsApp:** `connected`, `disconnected`, `reconnecting` (espelhados no `qr-connect`).
- **Prazos:** `deadline-badge` muda de tom — dentro do prazo (ok), alerta quando faltam poucas horas (warn) e atrasado (late) para "Conectar a celula" (24h) e fonovisita.
- **Decisao por Jesus:** `decision-modal` com fluxo celula (lider assume) ou fluxo visitante (consolidacao assume, prazo 24h).
- **Consolidacao:** `fila`, `detalhe`, `100-consolidadas` com selos de consolidacao individual e/ou UV.
- **Assinatura:** `active`, `past-due`, `plans`.
- **Permissoes:** `matrix`, `saved` (alteracao reflete no menu/dashboard na hora).
- **Telas bloqueadas:** `locked-em-breve` (capacitacao; UV bloqueada no MVP) com simbolo de relogio/cinza claro, sem navegar.
- **Assistente:** `assistant-panel` em closed/open/thinking, ciente do papel e restrito ao tenant.

### Dados exigidos pelo design
Requisitos de dados oficiais (id — campos-chave — stories):
- **data-user-login** — email*, password*. *(US-01)*
- **data-work-queue** — tipo*(visitante|atendimento|relatorio), titulo*, contexto, responsavel, status*(aberto|assumido|resolvido). *(US-15, US-16, US-26)*
- **data-next-actions** — responsavel*, papel, qtdAcoes*. *(US-17)*
- **data-conversations** — contato*, telefone*, ultimaMensagem, estado*(ia|humano|aguardando), naoLidas. *(US-08, US-11, US-12, US-13)*
- **data-human-queue** — contato*, esperaMin*. *(US-14)*
- **data-contacts** — nome*, telefone*, tipo*(visitante|membro|lider), celula, acompanhamento*(sem|em_andamento|consolidado), resumo de consentimentos por finalidade*, semInteresse (CSIM — delta Onda 1/#1), semInteresseMotivo. *(US-09, US-10, US-18, US-31, US-32, delta-052)*
- **data-contact-detail** — nome*, historico, celula. *(US-19, US-20)*
- **data-cells** — nome*, lider*, membros*, visitantes, diaReuniao. *(US-21)* (extensao: cobertura espiritual obrigatoria — delta-029)
- **data-cell-detail** — membros*, alertas. *(US-22, US-23)*
- **data-reports** — celula*, semana*, presentes, visitantes, status*(recebido|pendente). *(US-24, US-25, US-26)*
- **data-broadcasts** — titulo*, mensagem*, segmentos*, modo*(agora|agendado), data, hora, repeticao(once|daily|weekly|biweekly|monthly), alcance. *(US-33)*
- **data-consent** — contato*, finalidade*, versao*, estado*(concedido|retirado)*, registradoEm*, optoutGlobal*. *(US-31, US-32, delta-052)*
- **data-events** — titulo*, data*, hora. *(US-30)*
- **data-whatsapp-connection** — numero*, status*(online|offline|reconectando), ultimaSync. *(US-05, US-06, US-07)*
- **data-agent-config** — nome, tom, comportamento*(prompt), publicoAlvo, acessos*, ativo*. *(US-28)*
- **data-llm-credential** — provedor*, apiKey*(secret). *(US-27)*
- **data-crons** — nome*, frequencia*, ativo*. *(US-29)*
- **data-team** — nome*, email*, papeis*(admin|pastor|lider_g12|lider_consol|lider_celula|lider_mult|membro), status*(ativo|convidado). *(US-03, US-04)*
- **data-subscription** — plano*, status*(ativa|inadimplente), pessoas*, limite*, proximaCobranca. *(US-34, US-35, US-36)*
- **data-user-roles** — usuarioId*, papeis*. *(US-03, US-04)*
- **data-role-perms** — papel*, telas*. *(US-04)*
- **data-pipeline-stage** — pessoa*, etapa*(ganhar|consolidar|discipular|enviar), subetapa, presencasCelula, aceitouJesus. *(US-18, US-19)*
- **data-consolidacao** — pessoa*, tipo*(individual|universidade_vida), responsavel, progresso, concluida*. *(US-18, US-19)*
- **data-decision** — decisao por Jesus (fluxo celula/visitante, prazo 24h). *(US-37, US-40)*
- **data-trilha** — etapas da Capacitacao Destino por discipulo. *(US-18)* (tela bloqueada no MVP)
- **data-descendencias / data-multiplicacoes / data-aptos-lideranca / data-system-managers / data-assistant / data-tenants / data-provisioning** — suportam g12, enviar, gerentes, assistente e stubs Super-Admin.

### Restricoes de implementacao frontend
- Implementar **somente** telas, menus, componentes e estados definidos neste Design Lock; nao adicionar telas/fluxos fora do contrato.
- Respeitar tokens visuais (cores oklch, tipografia, espacamento, radii) e a direcao modern-minimal + tech utilitario, densidade balanced; numeros tabulares e bordas hairline.
- **RBAC sem seletor manual:** nao ha "Ver como"; menu e dashboard derivam da uniao dos papeis acumulados do cadastro; Configuracao so para `admin`. A matriz de Permissoes e a fonte de verdade dos acessos e reflete na hora.
- **Autorizacao real no backend:** a trava de UI e conveniencia; cada endpoint revalida `igreja_id` + papel. O agente usa as mesmas funcoes/validacoes de um humano (sem atalhos).
- **Telas bloqueadas no MVP:** `capacitacao` e `universidade-vida` em estado bloqueado ("em breve"), sem navegar para conteudo; sinalizam disponibilidade futura.
- **Super-Admin (US-42/US-43):** apenas stubs de rastreabilidade; nao implementar a gestao real no painel operacional — superficie separada.
- **Pendencias com prazo:** usar `deadline-badge` com mudanca de tom (dentro/alerta/atrasado); "Conectar a celula" carrega prazo de 24h.
- **Assistente do sistema:** `assistant-panel` deve ser role-aware e restrito ao tenant; no prototipo as respostas sao roteadas por palavra-chave (sem IA real), com contrato `api-assistant` previsto para producao.
- **Consentimento/Notificacoes:** o estado por contato e modelado como Notificacoes (Liberadas/Bloqueadas), usado para excluir do envio de comunicados.
- **Telas legadas** (`contatos`, `celulas`, `relatorios`) so via deep-link, fora do menu.

---

## Conflitos resolvidos

> Conflitos entre as stories aprovadas e o Design Lock travado. Onde a decisao do Design Lock esta rastreada para user story, ela e preservada.

1. **Super-Admin do SaaS (US-42, US-43) no painel da igreja.** As stories pedem gestao e provisionamento de igrejas; o Design Lock define isso como **superficie separada** do painel operacional e entrega apenas **telas de stub** (`super-admin-igrejas`, `super-admin-provisionar`) para rastreabilidade (delta-002/024). **Resolucao:** requisitos US-42/US-43 e RF-48/RF-49 preservados, porem fora do escopo do painel operacional do MVP (ver Escopo Negativo); a implementacao real ocorre em artifact/console proprio.

2. **Controle de acesso por papel (US-04) — papel unico vs. papeis acumulados.** A story descreve a hierarquia "quem esta acima ve o que esta abaixo". O Design Lock detalha o modelo como **papeis acumulados por pessoa** + **matriz Permissoes (papel x tela)** + **promocao automatica pela trilha** (delta-005/010/011/014/031/032), removendo o seletor manual "Ver como". **Resolucao:** decisao do Design Lock preservada (rastreada a US-03/US-04); refletida em RF-D1/RF-D2/RF-D3. Nao contradiz a hierarquia da story — a operacionaliza.

3. **Universidade da Vida e Capacitacao Destino (US-39 e cobertura US-18/US-19).** A story US-39 trata da conclusao da consolidacao (UV) e aptidao a CD. O Design Lock **bloqueia as telas** `universidade-vida` e `capacitacao` no MVP (`locked-em-breve`) e adota placeholder honesto do modelo oficial da CD (delta-019/028). **Resolucao:** o requisito de conclusao da consolidacao (US-39/RF-45) permanece atendido na `consol-individual`; o detalhamento de aulas/turmas de UV/CD fica no roadmap. Telas preservadas no contrato, porem bloqueadas.

4. **Consolidacao concluida nao matricula automaticamente na CD (US-39).** A story exige conclusao manual e entrada em "lista de aptos para a proxima turma". O Design Lock confirma: avanco da trilha governado por regras/estado, confirmacao gateada por identidade (so o consolidador confirma) e, ao concluir, a pessoa entra no criterio da proxima turma sem matricula automatica (delta-018). **Resolucao:** alinhado; sem conflito material — preservado em RF-45/RF-D3.

5. **"Sem consentimento" (US-31/US-33) remodelado como Notificacoes.** O Design Lock converteu o antigo conceito "sem consentimento" em estado global de **Notificacoes (Liberadas/Bloqueadas)** por contato (delta-008). **Resolucao atual:** o delta-052 substitui o consentimento unico por quatro finalidades. A UI precisa exibir cada finalidade e o opt-out global; o estado legado nao libera por inferencia cuidado pastoral, tarefas operacionais ou comunicados (RF-36/RF-38).

6. **Inbox restrito (US-11).** A story exige que lideres de celula NAO acessem o inbox. O Design Lock mantem o acesso restrito via papeis/permissoes (Configuracao e inbox condicionados a papel). **Resolucao:** alinhado; reforcado em RF-05/RF-D7.

7. **Agente IA em Configuracao (US-27/28/29).** A lista explicita de Configuracao do usuario nao citava "Agente IA"; o Design Lock o **mantem** em Configuracao (admin only) por ser requerido pelas stories e ja ter wiring, marcando como ponto a confirmar (delta-016). **Resolucao:** preservado em Configuracao (RF-30..RF-33); item aberto para confirmacao do posicionamento de menu, sem impacto em escopo funcional.

8. **Navegacao reorganizada em torno do ciclo G12 (delta-012).** As telas legadas `contatos`, `celulas` e `relatorios` saem do menu (deep-link valido), cobertas por `ganhar`, `central-celula` e estagios do ciclo. **Resolucao:** reorganizacao de navegacao, nao remocao de requisito — todas as US correspondentes (US-09, US-10, US-18..US-26) permanecem cobertas pelas novas telas.
   - **delta (UX 2026-06-23):** a tela `contatos` **volta ao menu** como **"Pessoas"** (grupo Igreja) — lista geral de todas as pessoas com filtro por papel/status (inclui CSIM) e edicao pelo admin (RF-22 estendido: PATCH passa a aceitar `sem_interesse`/motivo). A antiga `equipe` (gestao de acessos ao painel) vira **"Usuarios do Sistema"** e **move para Configuracao** (admin-only). O item `inbox` passa a se chamar **"Conversas"** (so rotulo). Reverte parcialmente o delta-012 (apenas `contatos` volta ao menu).

---

## Decisoes Tecnicas

### Database

> Decisoes de banco de dados e persistencia consolidadas pelo agente Database do Development Pipeline 2.0. Cada tabela/campo abaixo rastreia uma user story e/ou um `dataRequirement` do `design-contract.json`. Nenhuma tabela foi criada para tela/menu sem requisito associado. Telas bloqueadas no MVP (`universidade-vida`, `capacitacao`) e a superficie separada de Super-Admin (`super-admin-igrejas`, `super-admin-provisionar`) NAO geram tabelas operacionais nesta fase.

#### 1. Plataforma e princípios
- **SGBD:** PostgreSQL gerenciado pelo **Supabase** (US-02, RNF-02, dependencia externa do PRD).
- **Multi-tenant (RNF-21):** toda tabela de dominio nasce com a coluna `igreja_id uuid NOT NULL` referenciando `igreja(id)` e com **RLS habilitado** desde a 1a linha. A igreja piloto e o 1o registro.
- **Isolamento (decisao: Opcao A):** uma unica base com isolamento por linha via RLS. Politica padrao usa a funcao `current_igreja_id()` que le o claim de tenant do JWT emitido pelo Clerk (`auth.jwt()`). O backend FastAPI **revalida `igreja_id` + papel em cada endpoint** (RF-D7); a trava de UI/RLS e defesa em profundidade, nao substitui a checagem no backend. O agente de IA usa as mesmas funcoes/validacoes de um humano.
- **Autenticacao (RNF-01):** o banco NAO armazena senha. A identidade vem do Clerk; o vinculo e feito por `clerk_user_id`.
- **Segredos (RNF-03):** credencial LLM e chaves de integracao sao cifradas em repouso (coluna dedicada cifrada, ex.: `pgcrypto`/`vault` do Supabase) e nunca retornadas em texto claro.

#### 2. Convencoes globais (item 7 aprovado)
- **PK:** `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`.
- **Timestamps:** `created_at timestamptz NOT NULL DEFAULT now()` e `updated_at timestamptz` (atualizado por trigger) em todas as tabelas.
- **Soft-delete:** coluna `ativo boolean NOT NULL DEFAULT true` onde o dominio exige inativar (ex.: `celula`, `agente_config`, `agendamento`). Sem hard-delete no fluxo normal.
- **Valores fechados:** **enums nativos do Postgres** para todos os conjuntos fechados do contrato (etapa, status, tipo, papel, etc.).
- **Tenant:** `igreja_id` indexado em todas as tabelas; RLS por `igreja_id` em todas.
- **Mascaramento (RNF-06):** CPF e dados sensiveis mascarados em logs/visoes; campos sensiveis fora de tabelas de log.

#### 3. Modelo de dados (tabelas)

**Tenant e identidade**
- **`igreja`** (tenant raiz) — `id`, `nome`, `status` (`ativa|suspensa|aguardando_aprovacao`), `porte_limite int`. Rastreia: RNF-21, `data-tenants` (US-02). Cobre o tenant; o console Super-Admin (US-42/US-43) e superficie separada e NAO gera tabelas operacionais aqui.
  > ⚠️ Divergencia doc↔codigo (2026-06-15): no schema real (migrations 0001/0002) `igrejas` = `id, nome, status, plano, created_at` — **nao existe `porte_limite`** (o porte vem de `plano` / `subscriptions.limite`), e o enum `igreja_status` tem **4** valores (`ativa|suspensa|aguardando_aprovacao|inadimplente`). A migration `0010` adiciona a tabela de plataforma `platform_admins` (sem `igreja_id`), nao operacional.
- **`pessoa`** (modelo unificado — RNF-22/RNF-25) — `id`, `igreja_id`, `nome`*, `telefone`*, `email`, `genero`, `faixa_etaria`, `regiao`, `origem`, `tipo` (`visitante|membro|lider`), `etapa` (`ganhar|consolidar|discipular|enviar`), `subetapa` (`novo_contato|visitante|em_consolidacao|consolidado`), `acompanhamento` (`sem|em_andamento|consolidado`), `presencas_celula int`, `aceitou_jesus boolean`, `celula_id` (FK `celula`), `lider_id` (FK `pessoa` — auto-relacionamento do organograma G12, RNF-25), `apto_proxima_turma_cd boolean`, `apto_lideranca boolean`. Rastreia: `data-contacts`, `data-contact-detail`, `data-pipeline-stage`, `data-descendencias`, `data-aptos-lideranca` (US-09, US-10, US-18, US-19, US-20). **Unicidade `(igreja_id, telefone)`** para impedir contatos duplicados apos reconexao do WhatsApp (RNF-16). A maquina de estados (etapa/subetapa) e governada por regras (RNF-22/RF-D3); edicao manual so como excecao do admin.
- **`usuario_acesso`** (ponte com o painel) — `id`, `igreja_id`, `pessoa_id` (FK), `clerk_user_id`, `email`*, `status` (`ativo|convidado`). Rastreia: `data-team`, `data-user-roles` (US-01, US-03, US-04). Decisao do item 2: `pessoa` e unica e central; o acesso ao painel/Clerk e modelado por esta tabela leve apenas para quem tem login.
- **`pessoa_papel`** (N:N — RBAC acumulado) — `pessoa_id`, `papel` (`admin|pastor|lider_g12|lider_consol|lider_celula|lider_mult|membro`), `igreja_id`. Rastreia: `data-user-roles`, `data-team` (US-03, US-04, RF-D1).
- **`papel_permissao`** (matriz papel x tela — fonte de verdade) — `id`, `igreja_id`, `papel`, `tela` (id da tela). Rastreia: `data-role-perms` (US-04, RF-D2). Admin tem acesso implicito total; `dashboard` garantido a todos. Acesso efetivo = uniao das telas dos papeis da pessoa.
- **`gerente_sistema`** (papel operacional, distinto do ministerial) — `id`, `igreja_id`, `nome`*, `email`*, `papel_operacional` (`admin_sistema|operador`). Rastreia: `data-system-managers` (US-03, US-04).

> **[Fase 1 — divergência de implementação · 2026-06-14]** A entidade `gerente_sistema` / tabela `system_managers` foi **UNIFICADA em `pessoa_papel` (`user_roles`)**: `operador` virou papel normal (configurável na matriz de Permissões) e `admin_sistema` foi mapeado para o papel `admin` já existente. A tabela dedicada e o enum `papel_operacional` ficam **descontinuados** (migrados via `0008`/`0009`; drop em migration posterior). A tela **Gerentes de Sistema (`#gerentes`)** e o item `nav-gerentes` foram **removidos** — operadores passam a ser geridos em **Pessoas (`#equipe`)** + **Permissões (`#permissoes`)**. Conceder acesso ao painel passa a **vincular o `app_user`/`usuario_acesso` a uma `Pessoa` existente** (`app_users.pessoa_id`, já previsto em `usuario_acesso.pessoa_id`). `data-system-managers`/`api-system-managers` ficam obsoletos (absorvidos por `data-user-roles`/`data-team`).

**Celulas e organograma**
- **`celula`** — `id`, `igreja_id`, `nome`*, `lider_id`* (FK `pessoa`), `dia_reuniao`, `cobertura_espiritual` (obrigatoria — delta-029), `ativo`. Rastreia: `data-cells`, `data-cell-detail`, `data-descendencias` (US-21, US-22). Contagens de membros/visitantes derivadas de `pessoa.celula_id`.
- **`celula_alerta`** — `id`, `igreja_id`, `celula_id`, `pessoa_id`, `tipo`, `mensagem`, `tratado boolean`. Rastreia: `data-cell-detail` (alertas), US-23, RF-26 (gatilhos configuraveis, baixado ao tratar).
- **`multiplicacao`** — `id`, `igreja_id`, `celula_id`, `status` (`agendada|sem_agendamento|aprovada|concluida`), `data_prevista date`, `descendencia`. Rastreia: `data-multiplicacoes` (US-21, US-22, US-23). "Aptos a liderar" (`data-aptos-lideranca`) sao derivados de `pessoa.apto_lideranca`.

**WhatsApp, conversas e IA**
- **`whatsapp_conexao`** — `id`, `igreja_id`, `numero`*, `status` (`online|offline|reconectando`), `ultima_sync timestamptz`. Rastreia: `data-whatsapp-connection` (US-05, US-06, US-07). **Unica conexao ativa por igreja** (RF-07) via unique parcial em `igreja_id`.
- **`conversa`** — `id`, `igreja_id`, `pessoa_id`, `telefone`*, `estado` (`ia|humano|aguardando`), `nao_lidas int`, `ultima_mensagem`, `responsavel_id` (FK `pessoa`/`usuario_acesso`), `assumido_em timestamptz`. Rastreia: `data-conversations`, `data-human-queue` (US-08, US-11, US-12, US-13, US-14). Apenas o numero oficial e registrado (US-07/RF-09). *(delta — exclusão de conversa: `DELETE /conversations/{id}` faz **hard delete somente admin** — remove a conversa, suas mensagens (cascata via FK `mensagem.conversa_id`) e limpa a mídia no Storage (best-effort). Ação irreversível, com diálogo de confirmação no inbox.)* *(delta — Parte B do chat: painel de dados do contato à direita do inbox lê o detalhe da pessoa via novo `GET /contacts/{id}` (detalhe rico — endereço, faixa etária, etapa/subetapa G12, célula e líder por nome, consentimento, datas; **sem mudança de schema**); a edição reaproveita `PATCH /contacts/{id}` (admin). Leitura aberta aos papéis do inbox; edição restrita a admin.)* *(delta — chat US-13/US-14: `POST /conversations/{id}/read` zera `nao_lidas` ao abrir; `ConversationOut.assumidoPorNome` exibe quem assumiu (banner "em atendimento por X"); `POST /conversations/{id}/transfer` transfere o atendimento humano a outro membro com acesso ao inbox (admin transfere qualquer; o detentor transfere a sua; 422 se o destino não tiver acesso); `GET /conversations/{id}/photo` traz a foto de perfil do contato via Evolution `fetchProfilePictureUrl` (best-effort, cai em iniciais). O compositor também grava áudio pelo microfone (MediaRecorder → enviado como `tipo=audio`). Sem mudança de schema.)*
- **`mensagem`** — `id`, `igreja_id`, `conversa_id`, `direcao` (`entrada|saida`), `autor` (`ia|humano|contato`), `tipo` (`texto|imagem|arquivo|audio`), `conteudo`, `created_at`, `media_path`, `media_mime`, `media_nome`, `media_tamanho`. Rastreia: historico de `data-conversations` (US-11), RNF-24. *(delta — Etapa 2 do chat: `tipo` estendido de `texto|audio` para incluir `imagem|arquivo`; o binario da midia NAO fica no Postgres — vai para o Supabase Storage (bucket privado `whatsapp-media`), e `media_path` guarda o ponteiro. A leitura no painel e por URL assinada de curta duracao; envio/recebimento via Evolution em base64. Migration `0015_message_media.sql` (renumerada de 0012 na integração com a master-console).)* *(delta — Parte A do chat: colunas `autor_nome` (snapshot do nome de quem respondeu) e `enviado_por` (FK `usuario_acesso`, auditoria) — quando um humano assume, a mensagem registra QUEM enviou; o nome exibido vem da assinatura `usuario_acesso.chat_nome` (fallback: nome da conta). Migration `0016_message_author.sql` (renumerada de 0013 na integração com a master-console).)*
- **`ia_consumo`** (RNF-24, desde o dia 1 — item 4) — `id`, `igreja_id`, `conversa_id`, `modelo`, `tokens_entrada int`, `tokens_saida int`, `custo numeric`, `created_at`. Rastreia: RNF-24, base do BYO-LLM e da metrica de custo de IA por igreja (US-08, US-27).

**Fila de trabalho pastoral**
- **`tarefa`** (work-queue item) — `id`, `igreja_id`, `tipo` (`visitante|atendimento|relatorio`), `titulo`*, `contexto`, `responsavel_id`, `status` (`aberto|assumido|resolvido`), `prazo timestamptz`, `pessoa_id`, `ref_id` (id do objeto relacionado), `created_at`. Rastreia: `data-work-queue`, `data-next-actions` (US-15, US-16, US-17, US-26, US-40). O `prazo` alimenta o `deadline-badge` (ex.: "Conectar a celula" 24h e fonovisita). Indices em `(igreja_id, status, prazo)` e `(igreja_id, responsavel_id)`.

**Relatorios de celula**
- **`celula_reuniao`** — fonte canonica vigente do relatorio, com `igreja_id`, `celula_id`, `data`, `relatorio_status`, `relatorio_enviado_em`, `relatorio_enviado_por`, `oferta_valor`, `observacoes` e `relatorio_snapshot`. O requisito historico `relatorio_celula` foi absorvido por esta entidade e nao deve originar uma segunda tabela. Rastreia: `data-reports` (US-24, US-25, US-26, RF-D6, delta-052). O relatorio iniciado pelo WhatsApp somente pode gravar depois da confirmacao e pelo mesmo servico de dominio usado pelo painel.

**Consolidacao e decisao**
- **`decisao_jesus`** — `id`, `igreja_id`, `pessoa_id`*, `origem`, `vinculo` (`celula|visitante`)*, `celula_id`, `responsavel_id`, `prazo_conexao timestamptz`. Rastreia: `data-decision` (US-37, US-40). Visitante sem vinculo abre prazo de 24h (pendencia "Conectar a celula").
- **`consolidacao`** — `id`, `igreja_id`, `pessoa_id`*, `tipo` (`individual|universidade_vida`)*, `responsavel_id`, `progresso int`, `concluida boolean`*. Rastreia: `data-consolidacao` (US-18, US-19, US-37, US-38, US-39). Ao concluir (RF-45): marca `pessoa.apto_proxima_turma_cd = true`, sem matricula automatica.
- **`consolidacao_etapa`** — `id`, `igreja_id`, `consolidacao_id`, `etapa`, `concluida boolean`, `concluida_em`. Rastreia: registro de etapas de RF-45 (US-39).

**Calendario, comunicados e consentimento**
- **`evento`** — `id`, `igreja_id`, `titulo`*, `data date`*, `hora`, `descricao`, `google_event_id`. Rastreia: `data-events` (US-30), sincronizacao Google Calendar. *(expandido — delta-049: + `status` (`confirmado`/`a_confirmar`), `tipo`, recorrencia por dia da semana, `origem`, e campos de confirmacao publico/antecedencia/mensagem; ver `docs/design/AGENDA-EVENTOS-EVT0-decisao.md`.)*
- **`comunicado`** — `id`, `igreja_id`, `titulo`*, `mensagem`*, `segmentos jsonb`*, `modo` (`agora|agendado`)*, `data date`, `hora`, `repeticao` (`once|daily|weekly|biweekly|monthly`), `alcance int`, `agendamento_id` (FK). Rastreia: `data-broadcasts` (US-33, RF-D5). Envio exclui contatos com `optout` ou sem consentimento para `comunicados`.
- **`consentimento_finalidade_evento`** (D2B2a integrada e inativa; migration ainda nao aplicada em Supabase) — `id`, `igreja_id`, `pessoa_id`*, `finalidade` (`atendimento_solicitado|cuidado_pastoral|tarefas_operacionais|comunicados`)*, `estado` (`concedido|retirado`)*, `versao_termo`, `fonte` (`whatsapp_inbound|painel_autenticado`), `registrado_por_app_user_id`, `chave_idempotencia`, `sequencia`, `registrado_em timestamptz`. Rastreia: `data-consent` (US-31, US-32, delta-052). E append-only, exige operador somente para a fonte do painel no INSERT inicial, isola idempotencia por tenant e ordena cada stream no banco. A exclusao referencial posterior do AppUser pode anonimizar o operador via `ON DELETE SET NULL`, preservando o evento. A projecao efetiva usa `ausente|concedido|retirado|reaceite_necessario|bloqueado_optout_global`; o opt-out global prevalece e um novo termo exige re-aceite da finalidade aplicavel. Nao ha backfill do legado nem caller nesta fatia.
- **`purpose_consent_governance_envelope`** (D2B2b3A draft-only; esta missao nao aplicou a migration D2B2b3A, e DEV e PROD confirmaram a ausencia) — no maximo um envelope por `igreja_id`, com schema e status fixos, exatamente quatro rascunhos operacionais, revisao global e revisao por finalidade, ator interno de criacao e atualizacao e timestamps. O JSON aceita somente os oito campos factuais fechados do delta-053; nao possui hipotese juridica, decisao sobre menores, atestado, aprovacao, digest, catalogo ou writer. O acesso e exclusivo do caminho auditado do Console Master e nao concede autoridade ao runtime.

**Agente, credencial e agendamentos**
- **`agente_config`** — `id`, `igreja_id`, `nome`, `tom`, `comportamento text`* (prompt), `publico_alvo jsonb`, `acessos` (`array(enum)`: contatos, celulas, relatorios, calendario, comunicados, assinatura)*, `ativo boolean`*. Rastreia: `data-agent-config` (US-28).
- **`llm_credencial`** — `id`, `igreja_id`, `provedor`* (OpenAI), `api_key_cifrada`* (secret). Rastreia: `data-llm-credential` (US-27, RNF-03). Validada antes de ativar (RF-30); nunca exibida em claro. OpenRouter nao integra o PastorAI (delta-052).
- **`agendamento`** (cron generico — RNF-23) — `id`, `igreja_id`, `nome`*, `tipo_gatilho` (`horario|estado`)*, `frequencia` (`once|daily|weekly|biweekly|monthly` ou expressao), `condicao_estado jsonb` (ex.: prazo vencendo, meta atingida), `ativo boolean`*. Rastreia: `data-crons` (US-29), base do motor de SLAs; reutilizado por `comunicado` agendado.

**Assinatura**
- **`assinatura`** — `id`, `igreja_id`, `plano`*, `status` (`ativa|inadimplente`)*, `pessoas int`* (contagem de cadastro), `limite int`*, `proxima_cobranca date`, `asaas_id`, `setup_pago boolean`. Rastreia: `data-subscription` (US-34, US-35, US-36). Contagem de porte por pessoas cadastradas promove o plano automaticamente (RF-42).

#### 4. Cortes de escopo de persistencia (itens 6 aprovado)
- **Trilha/Universidade da Vida/Capacitacao Destino:** telas bloqueadas no MVP. NAO criamos `data-trilha` nem tabelas de turmas/aulas. O unico residuo persistido e a flag `pessoa.apto_proxima_turma_cd` (exigida por RF-45). Modelo dos 6 livros da CD fica no roadmap.
- **Super-Admin (US-42/US-43):** superficie separada. A tabela `igreja` ja representa o tenant; NAO criamos tabelas operacionais de `data-tenants`/`data-provisioning` no painel da igreja.
- **Assistente do sistema (`data-assistant`, US-41):** prototipo sem IA real e role-aware; o contexto (papeis, tenant, telas permitidas) e **derivado** de `usuario_acesso` + `pessoa_papel` + `papel_permissao`. NAO criamos tabela de sessao do assistente no MVP; quando houver backend real (`api-assistant`), avaliar persistencia de mensagens.

#### 5. Seguranca e integridade
- **RLS:** policies `SELECT/INSERT/UPDATE/DELETE` por `igreja_id = current_igreja_id()` em toda tabela de dominio (RNF-02/RNF-21).
- **Anti-duplicacao (RNF-16):** unique `(igreja_id, telefone)` em `pessoa`; webhooks idempotentes processados por worker de filas (RNF-17).
- **Indices:** `igreja_id` em todas; mais `(igreja_id, status, prazo)` em `tarefa`, `(igreja_id, estado)` em `conversa`, `(igreja_id, celula_id, data)` e `(igreja_id, relatorio_status, data)` em `celula_reuniao`, `(igreja_id, telefone)` em `pessoa`. A unicidade do slot de reunião usa `(igreja_id, celula_id, data, coalesce(hora, ''))` conforme a migration vigente.
- **Performance (RNF-09):** listas paginadas (keyset/offset) para responder ate 2s com 1.000 registros.
- **Auditoria:** `created_at/updated_at` em tudo; `ia_consumo` e `mensagem` como trilha de IA/conversa (RNF-24); dados sensiveis mascarados em logs (RNF-06).

---

### Security

> Decisoes tecnicas de Seguranca, Autenticacao e Permissoes consolidadas a partir das User Stories aprovadas e do Design Lock (`design-contract.json`). Principio geral: **negar por padrao** (deny-by-default) com defesa em profundidade. Nenhuma permissao foi criada sem story/tela correspondente.

#### S1. Autenticacao (US-01, RNF-01)
- Autenticacao **exclusivamente via Clerk** como Identity Provider. O sistema **nao armazena senhas proprias** nem implementa fluxo de credenciais local.
- Metodos de login conforme habilitado no Clerk (e-mail/senha e demais metodos).
- Mensagens de credencial invalida **nao revelam** se o e-mail existe (sem enumeracao de usuarios).
- Sessao invalida ou expirada redireciona para a tela de login (`#login`).
- Logout (`nav-logout`) encerra a sessao do Clerk e retorna a tela de login.

#### S2. Identidade, papel e tenant (fonte de verdade)
- **Clerk e a fonte de verdade** de identidade. O `igreja_id` (tenant) e o papel-base sao mantidos em metadata/claims do Clerk e propagados no **JWT**.
- O **Supabase consome esses claims** diretamente no RLS via `current_igreja_id()`, evitando join adicional por requisicao (single source of truth no token).
- A tabela `usuario_acesso` (ponte com o painel) liga `clerk_user_id` a `pessoa_id`/`igreja_id`; papeis ministeriais ficam em `pessoa_papel` (RBAC acumulado).
- Papeis-base do MVP (somente os que possuem story/tela): **Pastor/Admin**, **Lider de Celula**, **usuario de atendimento humano** (ex.: secretaria) e **equipe de Consolidacao**. A hierarquia RBAC G12 completa permanece **fora do escopo (pos-MVP)**.

#### S3. Modelo de autorizacao (RBAC hibrido) (US-03, US-04)
- **RBAC hibrido**: papeis-base no Clerk + **matriz de permissoes por tenant** persistida em `papel_permissao` (papel x tela), editavel pela tela **Permissoes** (`#permissoes`, `adminOnly`, `GET/PUT /roles/permissions`).
- Acesso efetivo = **uniao** das telas dos papeis da pessoa (`pessoa_papel`). Admin tem acesso implicito total; `dashboard` garantido a todos os papeis com login.
- Alteracoes na matriz refletem **em tempo real** no menu e no dashboard.
- **Gestao de usuarios** (US-03): convite por e-mail + papel via Brevo, listagem de ativos/pendentes (`status` ativo/convidado) e **revogacao de acesso**, restrito ao Pastor/Admin (telas `#equipe`, `#gerentes`).

#### S4. Isolamento multi-tenant (US-02, RNF-02, RNF-21)
- Isolamento em **nivel de banco via RLS do Supabase**: policies `SELECT/INSERT/UPDATE/DELETE` com `igreja_id = current_igreja_id()` em **toda** tabela de dominio.
- `igreja_id` indexado em todas as tabelas.
- Tentativa de acessar recurso (por ID) de outra igreja **nao revela existencia**: o backend responde como **nao encontrado** (404), nunca diferenciando "existe mas sem acesso".

#### S5. Defesa em profundidade (enforcement em 3 camadas) (RNF-05)
1. **Banco (RLS Supabase)** — barreira final e inviolavel de isolamento por tenant.
2. **Backend (FastAPI)** — checagem de papel/permissao em **toda requisicao**, inclusive endpoints de escrita; nunca confia apenas no front.
3. **Frontend (Next.js)** — visibilidade condicional de menus e acoes (esconder o que o papel nao pode usar). Camada de UX, **nunca** como unica protecao.

#### S6. Rotas/areas protegidas e restricoes
- **Telas adminOnly (Pastor/Admin)**: Conexao WhatsApp (`#whatsapp`), Agente IA (`#agente`), Assinatura (`#assinatura`), Gerentes de Sistema (`#gerentes`) e Permissoes (`#permissoes`).
- **Inbox / Chat do WhatsApp** (`#inbox`, US-11/US-12): acesso **apenas** a Pastor/Admin e usuarios explicitamente liberados ao atendimento humano (ex.: secretaria). **Lideres de celula NAO acessam** as conversas do WhatsApp da igreja.
- **Dashboard de Consolidacao** (`#consolidar`, `#consol-individual`, US-38): restrito a **equipe de Consolidacao e usuarios com privilegio**; demais papeis nao acessam.
- **Dashboard / Fila de trabalho** (`#dashboard`, US-15): visivel apenas a papeis com privilegio (Pastor/Admin e autorizados).
- **Escopo do Lider**: visualiza somente a(s) celula(s) sob sua responsabilidade (US-22); acoes restritas a Admin (configuracao, assinatura, gestao de usuarios) nunca aparecem para Lider.
- **Confirmacao gateada por identidade** (consolidacao individual): a confirmacao de cada etapa/encontro so e permitida quando **usuario logado == consolidador responsavel** pela pessoa; a central apenas supervisiona.

#### S7. Dados sensiveis e protecao em repouso (RNF-03)
- **Chave de API do provedor LLM (BYO)** (US-27): persistida em `llm_credencial.api_key_cifrada`, cifrada em repouso com **envelope encryption / chave gerenciada** (Supabase Vault ou KMS). **Nunca** retornada em texto claro apos salva; a UI expoe apenas o **status** ("ativa"/"invalida"). Validacao de funcionalidade da chave antes de marca-la como ativa (RF-30).
- **Mensagens de WhatsApp e dados religiosos**: tratados como **categoria especial** de dado pessoal, acesso restrito por papel (ver S6).
- **Dados de pagamento (Asaas)** (US-34): processados pelo provedor; o sistema **nao armazena** dados completos de cartao (delegado ao Asaas, referenciados por `asaas_id`).
- **Conversas pessoais do pastor** (US-07): nunca capturadas; somente o numero oficial e registrado (`whatsapp_conexao` / `conversa`).
- **Mascaramento (RNF-06):** CPF e dados sensiveis mascarados em logs/visoes; campos sensiveis fora de tabelas de log.

#### S8. Seguranca de webhooks e integracoes (US-07, RF-09)
- O webhook da **Evolution API** valida **assinatura/segredo** do remetente antes de processar.
- A mensagem so e aceita se pertencer ao **numero oficial do tenant** correspondente; mensagens de outros numeros sao descartadas.
- Processamento via **worker de filas** idempotente, com reprocessamento (RNF-17) e sem geracao de contatos duplicados na reconexao (unique `(igreja_id, telefone)`, RNF-16).

#### S9. Formularios com validacao (entrada confiavel)
- Validacao **server-side** obrigatoria (alem da client-side) em todos os formularios sensiveis: convite de usuario (e-mail/papel), credencial LLM, cadastro de celula, comunicado segmentado, evento, agendamentos (crons), lancamento de decisao e provisionamento (Super-Admin).
- Selecao de responsavel/celula sempre valida que o alvo **pertence ao tenant** do usuario (ex.: atribuir item de fila, conectar visitante a celula).

#### S10. Comunicacao, consentimento e privacidade (LGPD) (RNF-06, US-31/US-32/US-33)
- **Consentimento por finalidade:** iniciar uma conversa pode registrar somente `atendimento_solicitado`. Cuidado pastoral, tarefas operacionais e comunicados possuem concessao, retirada, versao e fonte independentes; um estado legado nao libera todas as finalidades.
- **Opt-out** disponivel pelo WhatsApp e prevalente sobre os consentimentos; contatos com `optout` ou sem consentimento para `comunicados` sao **excluidos automaticamente** de envios em massa.
- O detalhe do contato exibe os quatro estados e o opt-out global. Texto exato, versao, hipotese juridica, prova, menores, retencao, eliminacao e semantica de opt-out precisam de pacote aprovado por responsavel humano e validacao juridica ou do encarregado; o codigo apenas exige esse pacote e nao o inventa.
- **D2B2a integrada e inativa:** o ledger interno existe como migration, ORM, dominio e servico sem caller. No INSERT inicial, `whatsapp_inbound` exige ator nulo, enquanto `painel_autenticado` exige `registrado_por_app_user_id` da mesma igreja. A exclusao referencial posterior do AppUser pode anonimizar o operador via `ON DELETE SET NULL`, preservando o evento. A migration nao foi aplicada em Supabase DEV ou PROD.
- **D2B2b1 integrada e inativa:** objetos e funcoes puras, sem migration ou caller, exigem chave opaca gerada no servidor, aplicam RBAC deny-first e recusam todo `concedido` enquanto o pacote humano e juridico estiver ausente. Limpar opt-out ou usar a fonte do painel nao prova manifestacao do titular. O template D2B2b2 organiza as decisoes pendentes, mas nao as aprova nem autoriza catalogo ou writer.
- **D2B2b3A draft-only:** o Console Master pode preparar fatos e campos permitidos dos quatro rascunhos por igreja. O backend deriva tenant e ator da rota e da sessao; e-mail nao e autoridade. Hipotese juridica, declaracao de operacao baseada em consentimento, decisao sobre menores, atestados, aprovacoes, digest e registros nominais nao sao editaveis. Todo rascunho operacional permanece `DRAFT_NOT_APPROVED`; existem somente callers administrativos de rascunho, sem caller de aprovacao, ledger ou runtime.
- **Isolamento D2B2a:** o ledger usa RLS habilitada e forcada, tenant somente pelo GUC transacional `app.tenant_igreja_id`, ACL limitada a SELECT/INSERT e nenhuma inferencia a partir do consentimento legado.
- **Isolamento D2B2b3A:** `purpose_consent_governance_envelope` usa RLS habilitada e forcada, zero policy de Data API e nenhuma ACL fora do owner. O acesso ocorre somente pelo caminho auditado do Console Master, com tenant explicito. Catalogo, evidence store, writer, aplicacao em Supabase e D2C permanecem bloqueados ate aprovacao do pacote por finalidade.

#### S11. Estado de acesso negado
- Padrao unico: **403** no backend para acao sem permissao do papel; **404 / "nao encontrado"** para recurso de outro tenant (sem vazar existencia).
- No front, **empty state** padronizado ("Voce nao tem acesso a esta area"), sem expor dados ou IDs de outro tenant.

#### S12. Super-Admin (US-42, US-43)
- Superficie **separada** do painel operacional da igreja (console multitenant do provedor); a tabela `igreja` representa o tenant, sem tabelas operacionais de provisionamento no painel da igreja.
- Papel `super_admin` **nao herda** acesso aos dados operacionais de nenhum tenant; eventual acesso a dados de igreja ocorre apenas por fluxo **auditavel** e explicito.
- O Admin Master pode preparar rascunhos D2B2b3A apenas pela igreja explicitamente selecionada no Console. Esse privilegio nao o transforma em controlador, dono factual, encarregado ou revisor juridico e nao permite aprovar o proprio rascunho.
- Provisionamento de nova igreja (`POST /super-admin/tenants`) so apos verificacao de pagamento; cada igreja nasce como **tenant isolado** com dados e credencial de IA proprios.

#### S13. Transporte e infraestrutura (RNF-04)
- Todo o trafego cliente-servidor sobre **HTTPS/TLS**, com certificado automatico (Let's Encrypt via Coolify/Dokploy).
- Segredos de integracao (Evolution, Brevo, Asaas, Clerk, LLM) mantidos fora do codigo, em gestao de segredos do ambiente.

#### S14. Assistente do sistema no painel (US-41, RF-47)
- O assistente respeita o **papel/permissoes do usuario logado** e o **isolamento por tenant**: nunca expoe dados fora do escopo do usuario nem de outra igreja. Contexto derivado de `usuario_acesso` + `pessoa_papel` + `papel_permissao`.
