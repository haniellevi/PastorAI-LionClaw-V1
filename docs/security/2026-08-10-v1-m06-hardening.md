# V1-M06 — hardening de app, VPS e Supabase

**Data:** 2026-08-10

**Base:** `origin/main` em `3f085ec7228d770649b0d9041f0e16154fe37629`

**Branch:** `codex/v1-m06-hardening`

**Escopo:** implementação local e auditorias somente leitura. **Nenhuma mudança em PROD.**

## 1. Decisões desta onda

- O armazenamento atual do JWT continua em cookie legível por JavaScript com
  fallback em `localStorage`. Removê-lo isoladamente quebraria o contrato Bearer
  e a sessão compartilhada entre superfícies. BAIXO-010 permanece aceito,
  válido e parcialmente mitigado; a correção definitiva exige sessão HttpOnly
  apoiada pelo backend e uma migração de login própria.
- O frontend passa a emitir, nas respostas resolvidas pelas rotas e assets do
  Next:
  - `Content-Security-Policy: frame-ancestors 'none'`;
  - `Referrer-Policy: strict-origin-when-cross-origin`;
  - `Permissions-Policy: camera=(), geolocation=(), microphone=(self)`.
- A CSP é deliberadamente estreita nesta onda: bloqueia framing sem inventar
  allowlists de scripts, conexões, imagens ou PWA que poderiam quebrar o Next.
- Redirects automáticos de normalização do framework, como a remoção de barra
  final com resposta 308, acontecem antes dos headers configurados por rota e
  ficam explicitamente fora dessa cobertura. São respostas sem conteúdo; o
  smoke runtime registra status/localização e impede cobertura parcial. Uma
  política mais ampla exigiria interceptar a normalização em missão própria.
- Dependências e lockfile pertencem exclusivamente à M07; M06 não os altera.
- `backend/app/main.py` e `deploy/docker-compose.yml` não pertencem a M06 e não
  foram editados.

## 2. VPS — auditoria somente leitura

### Estado comprovado

- Login por chave SSH em modo não interativo: **PASS**.
- O único administrador interativo é `root`; não há usuário alternativo em
  `sudo`.
- Estado efetivo do SSH: `PermitRootLogin yes`, `PasswordAuthentication yes`,
  `PubkeyAuthentication yes`, `KbdInteractiveAuthentication no`.
- UFW/nftables bloqueiam entrada por padrão e liberam 22/80/443; portas 8000,
  8080 e 18002 estão presas ao loopback.
- Fail2ban não está instalado. Há tentativas de senha nos logs.
- Há atualizações pendentes e reboot requerido: kernel atual `6.8.0-134`, com
  `6.8.0-137` preparado para o próximo boot.
- Nenhum arquivo, pacote, serviço, firewall ou container foi alterado.

### Ordem segura para uma janela futura

1. Comprovar Browser Terminal/Emergency Mode no hPanel e mapear as três chaves
   autorizadas.
2. Criar usuário nominal com chave SSH própria e senha local forte, guardada no
   cofre operacional. Adicioná-lo ao grupo `sudo`; a autenticação SSH continua
   por chave, enquanto o sudo exige essa senha local de forma interativa.
3. Em uma segunda sessão, provar login por chave e executar `sudo -k`,
   `sudo -v`, `sudo -l` e `sudo true`. Validar a política antes da mudança com
   `visudo -cf /etc/sudoers` e, se houver arquivos próprios, com
   `visudo -cf /etc/sudoers.d/<arquivo>`. Não criar regra `NOPASSWD`.
4. Manter uma sessão root aberta, criar drop-in reversível do sshd com senha
   desabilitada, `PermitRootLogin prohibit-password`, `MaxAuthTries 3`,
   `LoginGraceTime 30` e X11 desabilitado.
5. Rodar `sshd -t`, conferir `sshd -T`, recarregar — não reiniciar — e testar
   duas novas conexões.
6. Em mudança separada, instalar fail2ban ou aplicar rate-limit no SSH.
7. Só depois de migrar as automações, considerar `PermitRootLogin no`.
8. Aplicar updates e reboot em outra janela; validar SSH, firewall, nginx,
   Docker, containers, health e release ativa.

**Rollback:** manter a sessão original aberta; remover/restaurar o drop-in,
validar com `sshd -t` e recarregar. Se necessário, usar Browser Terminal,
Reset SSH ou Emergency Mode da Hostinger. Restore de snapshot é último recurso.

## 3. Supabase PROD — auditoria somente leitura

Projeto auditado: `pffafnchtxbimpwyaczq`.

### RLS sem policies

Quatro tabelas estão com RLS habilitada e zero policies:

- `password_reset_tokens`;
- `platform_admins`;
- `platform_audit_log`;
- `platform_orchestrator`.

Isso já é fail-closed para papéis sujeitos a RLS. As três tabelas de plataforma
também não têm grants para `anon`/`authenticated`; `password_reset_tokens`
mantém grants padrão, mas a RLS bloqueia o acesso no estado auditado.

Grants e RLS são controles separados: grants determinam se o papel alcança a
tabela pela Data API; RLS determina quais linhas ficam visíveis depois disso.
O histórico mínimo não habilitava RLS em `password_reset_tokens`, ainda que o
estado vivo auditado a tivesse habilitada. A migration M06 agora executa
`ENABLE ROW LEVEL SECURITY` explicitamente em cada tabela existente, cria a
policy restritiva `service_role_bypass_only` e revoga ACLs de tabela de
`PUBLIC`, `anon` e `authenticated`. Também revoga por coluna `SELECT`,
`INSERT`, `UPDATE` e `REFERENCES` dessas três identidades em cada coluna das
quatro tabelas. A revogação de tabela cobre SELECT, INSERT, UPDATE, DELETE,
TRUNCATE, REFERENCES, TRIGGER e, no PostgreSQL 17, MAINTAIN. PostgreSQL também
remove as ACLs de coluna correspondentes ao revogar a ACL da tabela; as
revogações explícitas por coluna permanecem como defesa redundante e evidência
auditável da pós-condição.

A transação é `SERIALIZABLE` e exige PostgreSQL 16+ para inspecionar as opções
de membership. A pós-condição verifica privilégios **efetivos** de tabela e de
coluna, não somente grants diretos: `PUBLIC` e herança com `INHERIT` entram na
matriz. Ela percorre recursivamente caminhos por `SET ROLE` e `ADMIN OPTION`:
`ADMIN` é capacidade de reemitir a membership com `SET` habilitado, portanto
também conta como caminho potencial. A migration aborta se algum papel
alcançável for `SUPERUSER`, `BYPASSRLS` ou `CREATEROLE`, proprietário de uma
tabela fechada, ou ainda tiver privilégio efetivo de tabela/coluna sobre ela.
Membership sem `INHERIT`, `SET` e `ADMIN` não gera falso positivo; memberships
com `ADMIN`/`SET` são aceitas somente quando toda a cadeia alcançável é segura.

A migration não concede nem revoga privilégios de `service_role` e não altera
`BYPASSRLS`. Antes do revoke, captura a matriz efetiva já disponível a
`service_role`; se remover `PUBLIC` reduzir qualquer direito pré-existente do
backend, a migration aborta e reverte tudo, em vez de criar um grant novo.

### `current_igreja_id`

O advisor sinaliza `EXECUTE` para `authenticated` em uma função
`SECURITY DEFINER`. O estado coincide com a migration já versionada
`20260805_153000_security_definer_execute_hardening.sql`: esse é o único helper
tenant mantido para `authenticated`, pois as policies RLS dependem dele.
Revogar agora poderia zerar consultas legítimas. **Nenhuma migration nova foi
criada para essa função.**

### FK sem índice — dívida operacional deferida

O advisor lista 51 FKs sem índice de cobertura. A distribuição estimada em PROD
é muito desigual:

- `agent_conversation_logs(conversation_id)`: aproximadamente 67.762 linhas;
- `messages(enviado_por)`: 36;
- `events(confirmado_por)`: 31;
- `pessoas(arquivada_por)`: 19;
- duas FKs de `conversations`: 2 linhas;
- as outras 45: estimativa zero.

Adicionar 51 índices agora aumentaria custo de escrita e multiplicaria índices
sem uso. `agent_conversation_logs(conversation_id)` continua sendo o caso
material confirmado, mas sua migration foi retirada desta PR: não existe
executor autorizado comprovadamente capaz de combinar `CREATE INDEX
CONCURRENTLY` fora de transação com o ledger nativo do Supabase.

O gate futuro deve primeiro consultar `pg_class`/`pg_index` e confirmar nome,
colunas, definição, `indisvalid`, `indisready` e `indislive`. Objeto homônimo
com definição errada ou índice `INVALID` é falha, nunca idempotência; qualquer
remoção/recriação exigirá autorização própria. M06 não contém SQL de índice.
Os outros 50 avisos permanecem INFO e devem ser reavaliados por volume/consulta.

### Drift do ledger e executor bloqueado

O preflight do DEV identificou 66 migrations SQL locais e somente 31 entradas
no ledger atual. Além disso, `public.schema_migrations`, usado por
`backend/scripts/apply_migrations.py`, está divergente e não é um ledger seguro
para decidir o que executar. Não houve escrita no DEV para corrigir isso.

Enquanto o drift existir, ficam proibidos `supabase db push`,
`apply_migration`, SQL Editor ou qualquer executor genérico que descubra,
reaplique ou registre uma lista de migrations. Eles podem tratar histórico já
existente como pendente ou produzir um registro falso de sucesso. O
`backend/scripts/apply_migrations.py` desta PR não possui mais esse modo:
`apply` sem seleção explícita aborta antes de abrir conexão ou emitir SQL.

Nenhuma aplicação está autorizada nesta onda. Um gate futuro poderá executar
somente este arquivo, depois de conferir nome e hash exatos contra a branch
aprovada:

```text
20260810_031050_explicit_deny_policies_for_closed_tables.sql
    SHA-256: 1524fa0944dd3f4c259fa81f528570f8a4be5cff010515d4c44ac30a8df063c6
```

O executor de arquivo único agora está versionado, mas sua utilização continua
um gate separado de DEV/PROD. Ele não enumera nem reconcilia migrations
históricas para escrita e não cria `public.schema_migrations`. Antes de qualquer
escrita, exige que o ledger já exista, tenha o schema esperado, RLS habilitado,
nenhum privilégio efetivo de tabela ou coluna para
`PUBLIC`/`anon`/`authenticated`, e `service_role` com `BYPASSRLS`. A inspeção
inclui as ACLs herdadas e todos os caminhos por `SET ROLE` e `ADMIN OPTION` a
papéis que possam possuir, contornar RLS, criar papéis ou acessar o ledger.
Ledger ausente, inseguro, incompatível, com duplicidade ou com nomes
desconhecidos aborta sem DDL/DML.

`status` é o preflight genérico somente-leitura: falha fechado se a ordem do
ledger divergir do catálogo ou se houver mais de uma pendência. Isso torna o
estado atual de 66 arquivos locais versus 31 registros um bloqueio explícito,
sem SQL de migration e sem alteração do ledger.

Quando um gate futuro aprovar uma única migration, o comando deverá usar
somente o basename, o SHA-256 já registrado acima e a confirmação literal:

```bash
cd backend
python -m pip install --require-hashes -r requirements.lock
# O cofre/secret manager injeta esta variável no ambiente do processo.
# A CLI não aceita DSN em argv e não deve imprimir o destino.
: "${M06_MIGRATION_DATABASE_URL:?injete a URL aprovada no ambiente do processo}"
python scripts/apply_migrations.py apply \
  --migration 20260810_031050_explicit_deny_policies_for_closed_tables.sql \
  --sha256 1524fa0944dd3f4c259fa81f528570f8a4be5cff010515d4c44ac30a8df063c6 \
  --confirm APPLY
```

O operador não deve colar a URL nesta documentação nem na conversa. O runner
recusa path traversal, caminho absoluto, symlink da raiz ou do arquivo,
arquivo fora da raiz, basename ambíguo, hash divergente e confirmação ausente.
Ele ancora a leitura no descritor da raiz versionada e compara a identidade do
arquivo antes e depois de ler os bytes; uma troca de arquivo, inclusive para
symlink, aborta antes da conexão. A CLI não oferece `--database-url` e não
exibe a URL em sucesso ou erro. Qualquer erro de parsing é deliberadamente
sanitizado: ele não repete argumentos, valores de opções, DSNs, senhas, tokens,
hosts ou parâmetros de query recebidos. O único retorno de uso é uma mensagem
genérica estática; `--help` mostra somente os nomes das opções suportadas.

O scanner aceita somente um wrapper externo inequívoco `BEGIN … COMMIT`, com
`SET TRANSACTION ISOLATION LEVEL SERIALIZABLE` opcional e exatamente nessa
posição. Qualquer outro controle de transação, inclusive `COMMIT` ou `ROLLBACK`
inline/decorado, aborta em vez de quebrar a atomicidade. O executor preserva uma
única transação `SERIALIZABLE`: valida e bloqueia o ledger, executa a migration
e registra seu nome no ledger no mesmo commit. Em falha, rollback reverte tanto
o SQL da migration quanto o registro. Se o nome já constar no ledger válido,
encerra sem reaplicar.

A operação de índice está **ADIADA** para uma missão separada. Nenhum executor
autorizado foi comprovado e esta branch não sugere executar `CREATE INDEX
CONCURRENTLY` por `apply_migration`, SQL Editor, `supabase db push` ou
`backend/scripts/apply_migrations.py`. A missão futura deve escolher e provar o
executor em ambiente descartável/DEV antes de propor novo SQL versionado.

### Findings separados, sem alteração nesta missão

1. **`public.schema_migrations`:** o hardening de RLS, policies e grants dessa
   tabela é uma migration própria. Não foi incluído aqui para não combinar
   proteção do ledger com a política das quatro tabelas internas.
2. **`public.current_igreja_id()` com `SECURITY DEFINER`:** requer revisão
   dedicada de owner, `search_path`, dependências RLS e `EXECUTE`; não houve
   mudança de função nem de grants nesta missão.
3. **Reconciliação 66 locais versus 31 entradas:** exige inventário read-only de
   nome, hash, ordem e estado nativo antes de qualquer backfill. Não se pode
   reaplicar migrations históricas nem preencher o ledger automaticamente.

## 4. Gates DEV → PROD

1. Revisar e aprovar este PR; merge é gate separado.
2. Repetir advisors e preflight no Supabase DEV.
3. Confirmar separadamente no catálogo: existência das quatro tabelas,
   `relrowsecurity`, policies, ACLs de tabela (`relacl`) e de coluna (`attacl`),
   roles, owners e caminhos em `pg_auth_members` com `inherit_option` e
   `set_option`. Divergência interrompe a aplicação para revisão; não há
   correção automática de grants ou memberships globais.
4. Enquanto `public.schema_migrations` e o ledger nativo divergirem, não usar
   `apply_migration`, `db push`, SQL Editor ou outro executor genérico. Um gate
   separado deve confirmar que o ledger já foi endurecido e conferir o nome e
   SHA-256 registrados em §3 antes de qualquer escrita.
5. Só após esse gate, usar o executor de arquivo único descrito acima para a
   migration de policies identificada em §3. Confirmar a entrada no ledger
   nativo e abortar se qualquer preflight não coincidir. O executor não pode
   enumerar ou reaplicar o restante do histórico.
6. Provar em DEV: acesso service-role preservado sem grant novo,
   `anon`/`authenticated` bloqueados nas quatro tabelas e em cada coluna,
   `REFERENCES`/FK e `TRIGGER` negados, caminhos transitivos de `SET ROLE` e
   `ADMIN OPTION` seguros, reset de senha funcional, console master funcional e advisor de
   policies resolvido.
7. Publicar o frontend em preview e testar login/logout/refresh em `app`,
   `admin` e `painel`, além dos três headers nas páginas/assets. Registrar o
   redirect 308 de normalização como fora dessa cobertura.
8. Abrir missão separada para o índice somente depois de escolher um executor
   autorizado e provar transação, ledger, preflight de homônimo e recuperação
   de `INVALID`. Esta M06 não oferece comando de aplicação.
9. Repetir o preflight read-only em PROD. Aplicação em PROD exige autorização
   nova e explícita para cada migration.
10. Aplicar somente a migration de policies pelo mecanismo de arquivo único
   provado em DEV e
   repetir catálogo, grants, ledger, advisors e smokes.
11. Deploy do frontend em PROD é outro gate. A janela de SSH/VPS permanece
   separada de migrations e frontend.

## 5. Rollback

- Frontend: reverter o commit/deployment; os headers somem sem alterar sessão.
- Policies e grants: uma migration reversa pode remover
  `service_role_bypass_only`, mas não deve desabilitar RLS. Qualquer regrant a
  `anon`/`authenticated` exige o ACL prévio capturado no preflight e autorização
  própria; não há rollback automático de exposição à Data API.
- Índice: não há alteração nem rollback nesta branch; a dívida foi deferida.
- `current_igreja_id`, ledger, VPS e containers não são alterados por esta
  branch e não exigem rollback.

## 6. Evidência de validação

- Frontend: 62 arquivos / 520 testes passaram; lint, typecheck e build Next
  15.5.22 passaram.
- M06 não altera `package.json` nem `package-lock.json`; auditoria e atualização
  de dependências continuam sob ownership exclusivo da M07.
- Smoke HTTP de produção local: `app.`, `admin.` e `painel.` responderam 200;
  página, `icon.svg` e chunk Next receberam os três headers exatos. O redirect
  automático `/privacidade/` respondeu 308 para `/privacidade` e foi registrado
  como fora da cobertura dos headers de rota, sem política parcial.
- Backend offline: 1.973 testes passaram e 136 foram deselected pelo marker.
- Integração RLS completa em PostgreSQL 17 descartável: 174 testes passaram e
  1.973 foram deselected; o container foi removido ao final. Permaneceram dois
  warnings já conhecidos, sem falha de teste.
- O workflow `rls-integration` também executa
  `tests/test_apply_migrations.py` contra PostgreSQL descartável: cobre o
  bloqueio do modo genérico, ledger inseguro, seleção/hash, rollback do SQL e
  do ledger, ACLs e caminhos de membership. Assim essa proteção não depende de
  um teste local opt-in.
- Migration M06 isolada em PostgreSQL 17 temporário: 39 testes de integração
  passaram e um estrutural ficou deselected pelo marker. A migration foi
  aplicada e reaplicada; as quatro tabelas ficaram com `relrowsecurity=true`,
  uma policy restritiva `false/false`, sem privilégio efetivo de tabela ou
  coluna para `anon`/`authenticated`. Foram exercitados grants diretos e
  `PUBLIC` por coluna, grants mistos, FK via `REFERENCES`, `TRIGGER`,
   membership herdado, caminhos diretos/transitivos combinando `SET ROLE` e
   `ADMIN OPTION`, papel alcançável `BYPASSRLS`/`SUPERUSER`/`CREATEROLE`,
   proprietário alcançável e papel alcançável com ACL. Casos divergentes
   reverteram grants, RLS e policies integralmente;
  `service_role`/BYPASSRLS foi preservado sem regrant.
- `git diff --check`: passou.
- CodeGraph e Graphify não foram usados como evidência desta correção.

## 7. Referências operacionais

- [Supabase — Row Level Security](https://supabase.com/docs/guides/database/postgres/row-level-security)
- [Supabase — grants e RLS na Data API](https://supabase.com/docs/guides/api/securing-your-api)
- [Supabase — mudança de exposição automática](https://supabase.com/changelog/45329-breaking-change-tables-not-exposed-to-data-and-graphql-api-automatically)
- [Supabase — unindexed foreign keys](https://supabase.com/docs/guides/database/database-linter?lint=0001_unindexed_foreign_keys)
- [Supabase — database migrations](https://supabase.com/docs/guides/deployment/database-migrations)
- [Next.js — headers](https://nextjs.org/docs/app/api-reference/config/next-config-js/headers)
- [Next.js — redirects](https://nextjs.org/docs/app/api-reference/config/next-config-js/redirects)
- [Hostinger — diagnóstico de SSH](https://support.hostinger.com/en/articles/9969102-how-to-troubleshoot-ssh-connection-issues-on-vps)
- [Hostinger — Emergency Mode](https://support.hostinger.com/en/articles/5726577-how-to-use-emergency-mode-on-your-vps)
